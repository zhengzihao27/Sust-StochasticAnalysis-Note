% !TeX root = ../main.tex
\chapter{Intro}

\underline{Riemann-Stieltjes Integral}: If $f$ is continuous and $g$ is of \underline{finite variation} then $\int_{a}^{b} f(x) ~ \mathrm{d}g(x)$ is well-defined and is defined to be 
$$
    \int_{a}^{b} f(x) ~ \mathrm{d}g(x):=\lim_{n\to \infty} \sum_{i=0}^{n-1}f(\xi_{i})\left(g(x_{i+1}) - g(x_{i})\right)
$$
with $a \leq x_{0} < x_{1} < \cdots < x_{n} \leq b$ and $\xi_{i} \in [x_{i}, x_{i+1}]$.

\underline{Main Task}: It\^o integral $(H\cdot X)_{t} = \int_{0}^{t} H_{s} ~\mathrm{d}X_{s}$.

E.g $X$ is Brownian motion.

\thmnn{$\displaystyle\sum_{k=1}^{2^{n}}\left(B_{\frac{k}{2^{n}}} - B_{\frac{k-1}{2^{n}}}\right)^{2} \xlongrightarrow{a.s.} 1 .$}

Finite Variation of a function $g:[a,b] \to \mathbb{R}$ is 
$$
    V_{g}([a,b]) = \int_{a}^{b} \abs{ \mathrm{d}g(x)} = \sup_{n}\sum_{i=1}^{n}\abs{g(x_{i+1}) - g(x_{i})}
$$

\cornnp{$V_{B}([0,1]) = \infty$ a.s.}{
    \begin{equation*}
        \begin{aligned}
                    \textcolor{red}{(\star)}\sum_{k=1}^{2^{n}}\left( B_{\frac{k}{2^{n}}} - B_{\frac{k-1}{2^{n}}}\right)^{2} &\leq \sup_{1 \leq k \leq 2^{n}} \abs{B_{\frac{k}{2^{n}}} - B_{\frac{k-1}{2^{n}}}}\cdot \sum_{k=1}^{2^{n}}\abs{B_{\frac{k}{2^{n}}} - B_{\frac{k-1}{2^{n}}}}\\
        & \leq V_{B}([0,1]) \cdot \sup_{1 \leq k \leq 2^{n}}\abs{B_{\frac{k}{2^{n}}} - B_{\frac{k-1}{2^{n}}}}
        \end{aligned}
    \end{equation*}
    If $V_{B}([0,1]) < \infty$, then $\textcolor{red}{(\star)} \to 0$.
    
    Contradiction with $\textcolor{red}{(\star)} \to 1$, so w.p.1 $V_{B}([0,1]) = \infty.$
}

$(H\cdot X)_{t} = \int_{0}^{t} H_{s}~\mathrm{d}X_{s}$, where $H_{s}$ is previsible or predictable, $X_{s}$ is semi martingale.

\ex{}{Let $H_{n} \in \mathcal{F}_{n-1}$, $(M_{n})$ be a mart. 

Then $(H\cdot M)_{n} = \sum_{k=1}^{n}H_{k}(M_{k} - M_{k-1})$ is a martingale.}

\section{Set-up}

Given a probability space $(\Omega, \mathcal{F}, \mathsf{P})$. A stochastic process $X=(X_{t})_{t \geq 0}$ is a collection of random variables taking values in $(\mathbb{R}^{d},\mathcal{B}(\mathbb{R}^{d})).$
$$
\begin{aligned}
    X\colon [0,\infty) \times \Omega &\to \mathbb{R}^{d}\\
   (t,\omega) &\mapsto X_{t}(\omega).
\end{aligned}
$$

Fix $\omega$, then the function $t \mapsto X_{t}(\omega)$ is called a sample path.

\defnn{For two stochastic process $X$ and $Y$, we say $Y$ is a modification of $X$ if $$\forall~ t \geq 0,\quad \mathsf{P}(X_{t}= Y_{t}) = 1$$}

\defnn{We say $X$ and $Y$ are indistinguishable if 
$$\mathsf{P}(X_{t} = Y_{t},~ \forall~ t \geq 0) = 1.$$ 
And then $Y$ is a version of $X$.}

\ex{}{Let $Y$ be a modification of $X$ and suppose $X$ and $Y$ both have a.s. right continuous sample paths. Then $X$ \& $Y$ are indistinguishable.}

\defnn{A function f is right-continuous if $\displaystyle f(x)= f(x+) = \lim_{t\downarrow x} f(t)$. }

\pf{Since $\forall~  t \geq 0$, $\mathsf{P}(X_{t} \neq Y_{t}) = 0,$ we get
    $$
        \mathsf{P}\left(\bigcup_{q\in \mathbb{Q}^{+}} X_{q} \neq Y_{q}\right) \leq \sum_{q\in \mathbb{Q}^{+}} \mathsf{P} (X_{q} \neq Y_{q}) = 0.
    $$
    $$
        \Rightarrow~ \mathsf{P}\left(\bigcap_{q\in \mathbb{Q}^{+}} X_{q} = Y_{q}\right) = 1.
    $$
    For two right-continuous functions $f$ and $g$, if $f(q) = g(q)$, $\forall~ q \in \mathbb{Q}^{+}$, then $f(t) = g(t)$, $\forall~ t \geq 0.$

    Hence $\mathsf{P}\left(X_{t} = Y_{t},~ \forall~ t \geq 0\right) = 1.$
}

\ex{}{Let $T \sim \mathrm{Exp}(1)$. Define 
    $$
    \begin{aligned}
        &X_{t} \equiv 0,\quad\forall~ t \geq 0 \\
        &Y_{t} = \begin{cases}
            0,& t \neq T\\
            1,&t = T.
        \end{cases}
    \end{aligned}
    $$
    $\forall~  t \geq 0$, $\mathsf{P}(X_{t} \neq Y_{t}) = \mathsf{P}(Y_{t} = 1) = \mathsf{P}(t = T) = 0.$
}

\section{Filtration}

Let $(\mathcal{F}_{t})_{t \geq 0}$ be a filtration of $(\Omega, \mathcal{F})$ such that $\forall~  0 \leq s < t < \infty$, $\mathcal{F}_{s} \subseteq \mathcal{F}_{t} \subseteq \mathcal{F}.$

\ex{}{(Natural Filtration) Let $(X_{t})_{t \geq 0}$ be a stochastic process, Define
    $$
        \mathcal{F}_{t}^{X} = \sigma(X_{s},0 \leq s \leq t).
    $$
    Then $(\mathcal{F}_{t}^{X})$ is a filtration.
}
Obviously, $X_{t} \in \mathcal{F}_{t}^{X}.$

\exe{1.7}{Let $X$ be a process, every sample path of which is \underline{RCLL}. Let 
    $$
        A = \{\omega\colon X(\omega)\text{ is continuous on }[0,t_{0})\}
    $$    
    for some fixed $t_{0} > 0$. Show that $A \in \mathcal{F}_{t_{0}}^{X}.$
}

\underline{RCLL}: $f(x) = f(x+)$ and $f(x-)$ exists.

\pf{
    $$
    \begin{aligned}
        A &= \{\omega\colon \forall~ s\in (0,t_{0}) ,~ X(s-,\omega)= X(s,\omega) \}\\
        &=\{\omega\colon \forall~q \in (0,t_{0})\cap \mathbb{Q},~X(q-,\omega) = X(q,\omega) \}
    \end{aligned}
    $$
    Let $(0, t_{0})\cap \mathbb{Q} = (q_{m})_{m \geq 1}$, then 
    $$
    \begin{aligned}
        A &= \left\{
            \omega \colon \forall~ q_{m}\text{ with } m \geq 1,~\forall~k \geq 1,~\exists~ N \geq 1,~\forall~ n \geq N, \abs{X(q_{m}-\frac{1}{n},\omega) - X(q_{m},\omega)} < \frac{1}{k}
        \right\}\\
        &= \bigcap_{m=1}^{\infty}\bigcap_{k=1}^{\infty}\bigcup_{N=1}^{\infty}\bigcap_{n=N}^{\infty}\left\{\abs{X(q_{m} - \frac{1}{n}, X(q_{m})) < \frac{1}{k}}\right\}\in \mathcal{F}_{t_{0}}^{X}.
    \end{aligned}
    $$
}

\exe{1.8}{Let $X$ be a process whose sample paths are RCLL a.s. Then $A \notin \mathcal{F}_{t_{0}}^{X}$ is possible.}

\pf{
    [TODO]
}

\defnn{A filtration $\{\mathcal{F}_{t}\}_{t \geq 0}$ is said to satisfy the \underline{usual condition} if 
    \begin{enumerate}[label=(\alph*)]
        \item $\displaystyle \mathcal{F}_{t} = \mathcal{F}_{t+} = \bigcap_{s>t} \mathcal{F}_{s}$;
        \item $\mathcal{F}_{0}$ contains all the \underline{$\mathsf{P}-$negligible} sets in $\mathcal{F}$.
    \end{enumerate}
}

\defnn{An even A is $\mathsf{P}$-negligible if $\exists~N \in \mathcal{F}$ s.t. $A \subseteq N$, $\mathsf{P}(N) = 0$.}

\defnn{
    $\mathcal{F}_{t+} = \bigcap_{s >t}\mathcal{F}_{s}$, $\mathcal{F}_{t-}= \sigma\left(\bigcup_{s<t}\mathcal{F}_{s}\right)$. We say a filtration is right-continuous if $\mathcal{F}_{t} = \mathcal{F}_{t+}.$
}

\defnn{
    The stochastic process $X$ is \underline{adapted} to the filtration $\{\mathcal{F}_{t}\}_{t \geq 0}$ if $\forall~t \geq 0$, $X_{t} \in \mathcal{F}_{t}$.
}

Obviously $(X_{t})$ is adapted to the natural filtration.

\defnn{The stochastic process $X$ is called progressively measurable with respect to $(\mathcal{F}_{t})$ if $\forall~t \geq 0$, the map
    $$
        \begin{aligned}
            (s,\omega) &\mapsto X_{s}(\omega)\\
            ([0,t]\times \Omega,\mathcal{B}([0,t])\otimes \mathcal{F}_{t}) & \to (\mathbb{R}^{d}, \mathcal{B}(\mathbb{R}^{d}))
        \end{aligned}
    $$
    is measurable.
}

\rmk{adapted $\supseteq$ progressively measurable $\supseteq$ previsible
}



\propp{If $X$ is adapted to the filtration ($\mathcal{F}_t$) and every sample path is right-continuous or left-continuous, then $X$ is also progressively measurable.}{
    Assume right-continuity $\forall~ t > 0$, $\forall~  n \geq 1$, $\forall~  0 \leq k \leq 2^n -1,$
    $$
    X_s^{(n)}(\omega) = X_{\frac{k+1}{2^n}t}(\omega),\quad \forall~  \frac{kt}{2^n} < s \leq \frac{k+1}{2^n}t
    $$
    Then $\displaystyle \lim_{n \to \infty}X_{s}^{n}(\omega) = X_{s}(\omega)$ for all $(s,\omega) \in [0,t]\times \Omega.$

    It suffices to prove 
    $$
    \begin{aligned}
        (s,\omega) &\mapsto X_{s}^{(n)}(\omega)\\
        ([0,t]\times \Omega,\mathcal{B}([0,t])\otimes \mathcal{F}_{t}) & \to (\mathbb{R}^{d}, \mathcal{B}(\mathbb{R}^{d}))
    \end{aligned}
    $$
    is measurable.

    $\forall~ A \in \mathcal{B}(\mathbb{R}^{d})$,
    $$
    \begin{aligned}
        &\left\{(s,\omega)\colon X_{s}^{(n)}(\omega) \in A\right\}\\
        =&\bigcup_{k=0}^{2^{n} -1}\left(\frac{k}{2^{n}}t , \frac{k+1}{2^{n}}t \right]\times \left\{X_{\frac{k+1}{2^{n}}t} \in A\right\}\in \mathcal{B}([0,t])\otimes \mathcal{F}_{t}
    \end{aligned}
    $$
    We get $X_{s}^{(n)}(\omega)$ is measurable. Hence $X_{s} = \lim_{n\to \infty}X_{s}^{(n)}$ is also measurable.
}

\section{Stopping Times}
$(\Omega, \mathcal{F},\mathsf{P})$ and $(\mathcal{F}_{t})_{t \geq 0}$.

\defnn{A random time $T$ is a \underline{stopping time} of $(\mathcal{F}_{t})$ if $\forall~ t \geq 0$, $\{T \leq t  \} \in \mathcal{F}_{t}.$
}

\defnn{A random time $T$ is an \underline{optional time} of $(\mathcal{F}_{t})$ if $\forall~ t \geq 0$, $\{T < t\} \in \mathcal{F}_{t}.$
}

\propp{stopping time $\subseteq$ optional time and stopping $=$ optional if the filtration is right-continuous.}{
    ``$\Rightarrow$'' $\{T<t\} = \bigcup_{n=1}^{\infty}\{T \leq t - \frac{1}{n}\} \in \mathcal{F}_t$.
    
    ``$\Leftarrow$'' If $T$ is an optional time, then $\forall~  m \geq 1,$
    $$
        \{T \leq t\} = \bigcap_{n=m}^{\infty}\left\{T < t+\frac{1}{n}\right\}.
    $$
    Because $\{T < t + \frac{1}{n}\} \in \mathcal{F}_{t+\frac{1}{n}} \subseteq \mathcal{F}_{t+\frac{1}{m}}$.
    
    $\Rightarrow~ \{T \leq t\} \in \mathcal{F}_{t+\frac{1}{m}}$
    
    $\Rightarrow~ \{T \leq t\} \in \bigcap_{m=1}^{\infty}\mathcal{F}_{t+\frac{1}{m}}= \mathcal{F}_{t^+} = \mathcal{F}_t$.
}

\lemnn{If $T$ and $S$ are stopping times, then $T \wedge S$, $T \vee S $, $T+S$ are stopping times.}

\pf{$\forall~ t \geq 0$, 
    \begin{enumerate}
        \item $\{T \wedge S \leq t\} = \{T \leq t\} \cup \{S \leq t\} \in \mathcal{F}_{t}.$
        \item $\{ T \vee S \leq t\} = \{T \leq t\}\cap \{S \leq t\} \in \mathcal{F}_{t}.$
        \item $\{T+S \leq t\} \in \mathcal{F}_{t} \Leftrightarrow \{T+S > t\} \in \mathcal{F}_{t}$.
        $$
        \begin{aligned}
            &\{T + S > t\} \\
            = &\{T = 0,~S >t\}\cup \{S=0,~ T> t\}\cup\{0<T<t,~T+S > t\}\cup \{S>0,~ T \geq t\}\\
             \in &\mathcal{F}_{t} \cup \mathcal{F}_{t} \cup \{0<T<t,~T+S > t\} \cup  \mathcal{F}_{t}.
        \end{aligned}
        $$
        It suffices to show
        $$\{0< T < t ,~ T+S > t\} = \bigcup_{\stackrel{r\in \mathbb{Q}^{+}}{0 < r <t}}\{r < T < t,~ S> t-r\}.
        $$
        Because the right term belong $\mathcal{F}_{t}.$
        
        ``$\Leftarrow~$'' $S > t-r > t - T$, so $S+T > t$.

        ``$\Rightarrow~$'' $\exists~ r\in (0,T)$, $S+r > t$ since $S+T > t.$
    \end{enumerate}
}

\lemnn{If $\{T_n\}_{n \geq 1}$ are stopping times, then $\sup_{n \geq 1} T_n$ is a stopping time.}

\pf{
    $\forall~ t \geq 0,$ 
    $
        \{\sup_{n \geq 1} T_n \leq t\} = \bigcap_{n=1}^{\infty}\{T_n \leq t\} \in \mathcal{F}_t.
    $
}

\defnn{The stopping $\sigma$-algebra is $\mathcal{F}_T = \{A \in \mathcal{F} \mid \forall~ t \geq 0, A \cap \{T \leq t\} \in \mathcal{F}_t\}$.}

\prop{If $T(\omega) = t_0$ for some fixed $t_0 \geq 0$, $\forall~ \omega \in \Omega$, then $\mathcal{F}_T = \mathcal{F}_{t_0}$.}

\pf{Note that $\forall~  t \geq 0$,
    $$
        \{T \leq t\} = \begin{cases}
            \varnothing,& t_0 > t\\
            \Omega, &t_0 \leq t
        \end{cases}
    $$

    \noindent``$\mathcal{F}_{t_{0}} \subseteq \mathcal{F}_{T}$''

    $\forall~  A \in \mathcal{F}_{t_{0}}$, $\forall~  t \geq 0$,
    \begin{enumerate}[label=\textcircled{\arabic*}]
        \item $t \geq t_{0}$, $A \cap \{T \leq t\} = A \in \mathcal{F}_{t_{0}} \subseteq \mathcal{F}_{t};$
        \item $t < t_{0}$, $A \cap \{T \leq t\} = \varnothing \in \mathcal{F}_{t}$.
    \end{enumerate}
    Hence $A \in \mathcal{F}_{T} \Rightarrow~ \mathcal{F}_{t_{0}}\subseteq \mathcal{F}_{T}.$

     \noindent``$\mathcal{F}_{T} \subseteq \mathcal{F}_{t_{0}}$''

    $\forall~  A \in \mathcal{F}_{T}$, we have $A \cap \{T \leq t\} \in \mathcal{F}_{t}$ by definition. Let $t = t_{0}$, then $\{T \leq t_{0}\}= \Omega$, so

    $$
        A \cap \{T \leq t_{0}\} \in \mathcal{F}_{t_{0}} \Rightarrow~ A \cap \Omega \in \mathcal{F}_{t_{0}} \Rightarrow~ A \in \mathcal{F}_{t_{0}}.
    $$
    Hence $A \in \mathcal{F}_{t_{0}} \Rightarrow~ \mathcal{F}_{T}\subseteq \mathcal{F}_{t_{0}} $
}
    
\lemnn{$\forall~$ stopping time $T$ and $S$, $\forall~A \in \mathcal{F}_S$, we have $A\cap \{S \leq T\} \in \mathcal{F}_T.$ In particular, if $S \leq T$ for all $\omega \in \Omega$, then $\mathcal{F}_S \subseteq \mathcal{F}_T.$}

\pf{
    $T \wedge t$ is also a stopping time. $\{T \wedge t \leq s\} \in \mathcal{F}_s \subseteq \mathcal{F}_t.$ Hence $T \wedge t \in \mathcal{F}_t.$

    To prove $A\cap \{S \leq T \} \in \mathcal{F}_T$, we need to prove $\forall~ t \geq 0$, $A \cap \{S \leq T\} \cap \{T \leq t\}\in \mathcal{F}_t.$

    We have 
    $$
    A \cap \{S \leq T\} \cap \{T \leq t\} = \underbrace{A \cap \{S \leq t\}}_{\in \mathcal{F}_t} \cap \underbrace{\{T \leq t\}}_{\in \mathcal{F}_t} \cap \underbrace{\{S \wedge t \leq T \wedge t\}}_{\mathcal{F}_t}.
    $$
    ($S \leq T $ on $S \leq t$, $T \leq t \Leftrightarrow \{S \wedge t \leq T \wedge t\}$)
}

\lemnn{
    Let $S,T$ be two stopping times. Then $\mathcal{F}_{T \wedge S} = \mathcal{F}_T \cap \mathcal{F}_S$ and $\{T < S\}$, $\{S < T\}$, $\{T \leq S\}$, $\{S \leq T\}$, $\{T = S\}$ belong to $\mathcal{F}_S \cap \mathcal{F}_T = \mathcal{F}_{S \wedge T}.$
}

\pf{
    $\mathcal{F}_{T \wedge S} \subseteq \mathcal{F}_S$ and $\mathcal{F}_{T \wedge S} \subseteq \mathcal{F}_T \Rightarrow~\mathcal{F}_{T \wedge S} \subseteq \mathcal{F}_T \cap \mathcal{F}_S$.
    
    To prove $\mathcal{F}_T \cap \mathcal{F}_S \subseteq \mathcal{F}_{T \wedge S}$, $\forall~ A \in \mathcal{F}_T \cap \mathcal{F}_S$, we will prove $\forall~ t \geq 0$, $A \cap \{T \wedge S \leq t\} \in \mathcal{F}_t.$

    $$
    \begin{aligned}
        A \cap \{T \wedge S \leq t\} &= A \cap \left(\{T \leq t\} \cup \{S \leq t\}\right)\\
        &= \left(A \cap \{T \leq t\}\right)\cup \left(A \cap \{S \leq t\}\right)\in \mathcal{F}_t.
    \end{aligned}
    $$

    Next $\{S \leq T\}\in \mathcal{F}_T$, $\{S > t\}\in \mathcal{F}_T$. It remains to prove $\{S <T\} \in \mathcal{F}_T.$

    Assuming this, by interchanging $T$ and $S$, we get $\{T \leq S\}$, $\{T > S\}$, $\{T <S\}\in \mathcal{F}_S$.

    Then $\{S \leq T\} \in \mathcal{F}_T$, $\{S > T\}^\complement \in \mathcal{F}_S.$

    $\Rightarrow~ \{S \leq T\} \in \mathcal{F}_S \cap \mathcal{F}_T.$

    $\{T= S\} = \{ S \leq T\}\cap \{T \leq S\}\in \mathcal{F}_S \cap \mathcal{F}_T$.

    To prove $\{S < T\}\in \mathcal{F}_T$, we define $R = S \wedge T$ such that $\{S < T\}= \{R < T\}$. Notice that $R \in \mathcal{F}_R \subseteq \mathcal{F}_T$. Hence $\{R < T\} \in \mathcal{F}_T.$
}

\textbf{Summary:}
\begin{enumerate}[label=(\roman*)]
    \item $T \in \mathcal{F}_T$;
    \item If $S \leq T$, $\mathcal{F}_S \subseteq \mathcal{ F}_T$;
    \item $T \wedge t \in \mathcal{F}_{T \wedge t} \subseteq \mathcal{F}_t$;
    \item $\mathcal{F}_{T \wedge S} = \mathcal{F}_T \cap \mathcal{F}_S.$
\end{enumerate}

For each $\omega \in \Omega$, $T(\omega) \in [0,\infty)$ and $X_t(\omega) \in \mathbb{R}^d$, $\forall~ t \geq 0$, we define $X _T(\omega) = X _{T(\omega)}(\omega).$

\prop{
    Let $\{X_t\}_{t \geq 0}$ 
    be a progressively measurable process, Let $T$ be a stopping time. Then $X_T$ defined on $\{T < \infty\}$ is $\mathcal{F}_T$-measurable and the stopped process $\{X _{T \wedge t}\}_{t \geq 0}$ is progressively measurable.
}

\pf{
    $\forall~B \in \mathcal{B}(\mathbb{R}^d)$, $\{X_T \in B\} \in \mathcal{F}_T \Leftrightarrow$ $\forall~ t \geq 0$, $\underbrace{\{ X _T \in B\}\cap \{T \leq t\}}_{=\{X _{T \wedge t} \in B\} \cap \{T \leq t\}}\in \mathcal{F}_t$.

    We only need to prove $X_{T \wedge t} \in \mathcal{F}_t.$

    Observe $(s,\omega)\to (T(\omega) \wedge s, \omega)$ is $\mathcal{B}([0,t])\otimes \mathcal{F}_t$-measurable.

    By assumption, $(s,\omega)\mapsto X_s(\omega)$ is $\mathcal{B}([0,t])\otimes \mathcal{F}_t$-measurable.

    So $(s,\omega)\to(T(\omega)\wedge s, \omega)\to (X_{T(\omega) \wedge s},\omega)$ is $\mathcal{B}([0,t])\otimes \mathcal{F}_t$-measurable.
}

\section{Cts-time martingale}
\defnn{For $(\Omega,\mathcal{F},(\mathcal{F}_{t})_{t \geq 0},\mathbb{P})$, a martingale is an adapted stochastic process $(X_t)_{t \geq 0}$ such that
    $$
    \begin{cases}
        (i)~ \forall~ t \geq 0,~\mathbb{E}|X_t| < \infty \\
        (ii)~ \forall~ t \geq s \geq 0,~\mathbb{E}(X_t \mid \mathcal{F}_s) = X _s.
    \end{cases}
    $$

    If $\mathbb{E}(X_t \mid \mathcal{F}_s) \geq X _s$, we call it a submartingale.

    If $\mathbb{E}(X_t \mid \mathcal{F}_s) \leq X _s$, we call it a supmartingale.
}

\prop{Let $\varphi: \mathbb{R} \to \mathbb{R}$ be a convex (concave up) function. If $(X_t)_{t \geq 0}$ is a martingale, then $(\varphi(X _t))_{t \geq 0}$ is a submartingale.}

\textbf{E.g.} $\abs{X_t}$, $X _{t}^2$, $\abs{X _t}^p$ for all $p \geq 1$, $(X _t)^+$ are all submartingale.

\rmk{We assume $\mathbb{E}\abs{\varphi(X_t)}<\infty$, $\forall~ t \geq 0$.}

\pf{
    Jensen's inequality.
}

\thmnn{Continuous-time Doob's upcrossing inequality}

$$
\begin{aligned}
&T_{b}^{(0)} = -\infty,\\
& T_a^{(1)} = \inf \{t \geq (T _b^{(0)})^+\colon X _t \leq a\},\\
&T_b^{(1)} = \inf \{t \geq T _a^{(1)}\colon X _t \geq b\}.
\end{aligned}
$$

$\forall~ l \geq 2$,
$$
\begin{aligned}
    &T_a^{(l)} = \inf \{t \geq T _b^{(l-1)} \colon X _t \leq a\},\\
    &T_b^{l} = \inf \{ t \geq T _a^{l}\colon X _t \geq b\}
\end{aligned}
$$

Define $U_{ab}^X [0,n] = \max \{k \geq 0\colon T_{b}^{(k)} \leq n\}$ to be the \# of upcrossing in an interval [0,n].

\thmnn{Let $(X_{t})_{t \geq 0}$ be a right-continuous submartingale. For all $T > 0$,
    $$
    \mathbb{E}\left(U_{ab}^X [0,T]\right) \leq \frac{\mathbb{E}(X_{T} -a )^+}{b-a}
    $$
}

\pf{
    For each $n \geq 1$, we let $D_n = \mathbb{Z}/ 2^n = \{0, \frac{\pm 1}{2^n}, \frac{\pm 2}{2^n},\cdots\}$.

    Consider the upcrossing of $X$ on $[0,T]\cap D_{n}$, $\left\{\frac{X_{j}}{2^n}\right\}_{j=0}^\infty$ is a discrete-time martingale. Then $U_{ab}^X\left([0,T]\cap D_{n}\right) \leq U_{ab}^X [0,T]$,
    $U_{ab}^X\left([0,T]\cap D_{n}\right) \uparrow U_{ab}^X([0,T])$.

    By using discrete-time upcrossing
    $$
        \mathbb{E}\left(U_{ab}^X\left([0,T]\cap D_{n}\right)\right) \leq \frac{1}{b-a}\mathbb{E}(X_{T_n} - a)^+,
    $$
    where $T_n = \frac{[T \cdot 2^n]}{2^n}$. Let $n\to \infty$ to get
    $$
        \mathbb{E}\left(U_{ab}^X([0,T])\right) \leq \frac{1}{b-a}\mathbb{E}(X_{T} - a)^+.
    $$  
}

\prop{
    If $(X_{t})_{t \geq 0}$ is a right continuous sub-martingale, and $\sup_{t \geq 0} \mathbb{E} X_{t}^+ < \infty$, then $\exists~ X \in L^1$ s.t. $X_t \xlongrightarrow{a.s.}X$ as $t \to \infty.$ 
}

\pf{
    $$
    \begin{aligned}
        \mathbb{E}\left(U_{ab}^X([0,\infty))\right) &= \lim_{n \to \infty} \mathbb{E}\left(U_{ab}^X([0,n])\right)\\
        &\leq \limsup_{n\to \infty}\frac{1}{b-a}\mathbb{E}(X_{n} - a)^+ \\
        &\leq \limsup_{n\to \infty}\frac{1}{b-a}\left(\mathbb{E}(X_n^+)+ \abs{a}\right) < \infty.
    \end{aligned}
    $$
    Hence $\forall~ a < b$, $U_{ab}^X[0,\infty)< \infty$ a.s. 
    
    $\Rightarrow~$w.p.1, $\forall~ a < b \in \mathbb{Q}$, $U_{ab}^X[0,\infty) < \infty$.

    Hence $\liminf_{t\to \infty}X_{t} \leq a \leq b < \limsup_{t \to \infty} X_t$ does not occur.

    $\Leftrightarrow \liminf_{t\to \infty} X_{t} = \limsup_{t \to \infty}X_t.$
}

\defnn{
    A family of random variables $(X_t)_{t \geq 0}$ is uniformly integrable (U.I.) if 
    $$
        \lim_{M \to \infty}\sup_{t \geq 0} \mathbb{E}\left(\abs{X_t} \mathsf{1}_{\abs{X _t} > M}\right) = 0.
    $$
}

\rmkb{If $\sup_{t \geq 0} \mathbb{E} \abs{X_t}^p < \infty$ for some $p > 1$, then U.I.}

\thmnn{
    If $X_n \xlongrightarrow{P} X$, then equivalent
    $$
    \begin{cases}
        X_n \xlongrightarrow{L^1} X, \\
        \mathbb{E}\abs{X_n} \to \mathbb{E}\abs{X},\\
        \{X_n\} \text{ is U.I.}.
    \end{cases}
    $$
}

\prop{Let $Z \in L^1$. Then $\{\mathbb{E}(Z\mid \mathcal{G}) \colon \mathcal{G}\text{ is a sub }\sigma \text{-field of }\mathcal{F}\}$ is U.I.

In particular $\{\mathbb{E}(Z \mid \mathcal{F}_t)\}_{t \geq 0}$ is U.I. and it is a martingale. (By Tower's property)
}

\prop{
    A right continuous martingale $(X_t)_{t \geq 0}$ is U.I. if and only if $\exists~ X _{\infty} \in L^1$ such that $X _t = \mathbb{E}(X _\infty \mid \mathcal{F}_t).$
}

\pf{
    ``$\Rightarrow$'' U.I. implies $\sup_{t \geq 0}\mathbb{E}\abs{X_t} < \infty.$

    Then the martingale convergence theorem implies that $\exists~X_{\infty}$ s.t. $X_n \xlongrightarrow{a.s.} X$ and $\mathbb{E}\abs{X _{\infty}} = \mathbb{E}\left(\lim _{t \to \infty} \abs{X _t}\right) \leq \liminf _{t \to \infty} \mathbb{E}\abs{X _t} < \infty$.

    To prove $\forall~ t \geq 0$, $X_t = \mathbb{E}(X _{\infty} \mid \mathcal{F}_t) \Leftrightarrow \forall~A \in \mathcal{F}_t$, $\mathbb{E}(X _t \mathsf{1}_A)=\mathbb{E}(X _{\infty}\mathsf{1}_A)$.

    For any $s \geq 0$, we have $\mathbb{E}(X_{t+s} \mid \mathcal{F}_{t}) = X_{t} \implies \mathbb{E}(X_{t+s}\mathsf{1}_{A}) = \mathbb{E}(X_{t}\mathsf{1}_{A})$
    $$\abs{\mathbb{E}(X_{t+s}\mathsf{1}_A) - \mathbb{E}(X_{\infty} \mathsf{ 1}_A)} \leq \mathbb{ E}\abs{X_{t+s} - X_{\infty}}\abs{\mathsf{1}_A} \leq \mathbb{E}\abs{X_{t+s}-X_{\infty}} \to 0.$$

    Let $s\to \infty$, $\mathbb{E}(X_{t+s} \mathsf{1}_{A}) \xlongrightarrow{a.s.} \mathbb{E}(X_{\infty} \mathsf{1}_{A})$.

    Recall U.I. + a.s. $\implies L^1$ convergence.

    ``$\Leftarrow$'' If $\exists~ X_\infty \in L^1$ s.t. $X_t = \mathbb{E}(X_\infty \mid \mathcal{F}_t)$, then $\{X_t\}_{t \geq 0}$ is U.I.
}

\thm{Optional sampling Theorem}{
    Let $(X_t)_{t \geq 0}$ be a right continuous martingale. Let $S \leq T$ be two stopping time. Suppose either
    \begin{enumerate}[label=\textcircled{\arabic*}]
        \item $\{X_t\}$ U.I.,
        \item $\exists~$constant $K >0$, $S,T \leq K$.
    \end{enumerate}
    Then $\mathbb{E}(X_T \mid \mathcal{F}_S) = X _S \implies \mathbb{E}(X _T) = \mathbb{E}(X _S) = \mathbb{E}(X _0).$
}

\pf{
    Define \textcircled{1} $Z = X_\infty$, \textcircled{2} $Z=X_{K}$.

    In either case, $X_t = \mathbb{E}(Z\mid \mathcal{F}_t)$, \textcircled{1} $\forall~ t \geq 0$, \textcircled{2} $\forall~ t \leq K$.

    It suffices to prove $X_T = \mathbb{E}(Z \mid \mathcal{F}_T)$. Then $X _S = \mathbb{E}(Z \mid \mathcal{F}_S)$.

    Hence 
    $$
    \begin{aligned}
    \mathbb{E}(X_T \mid \mathcal{F}_S) &= \mathbb{E}\left(\mathbb{E}(Z \mid \mathcal{F}_T) \mid \mathcal{F}_S\right)\\
    &= \mathbb{E}(Z \mid \mathcal{F}_S)\\
    &= X_S.
    \end{aligned}    
    $$

    \textcircled{1} Suppose $T \in \{t_1,t_2,\dots\}$. Then $\forall~ A \in \mathcal{F}_T$, 
    $$
    \begin{aligned}
    \mathbb{E}(X_T \mathsf{1}_A) &= \mathbb{E}(Z \mathsf{1}_A) \\
    &= \sum_{n=1}^{\infty}\mathbb{E}(X_{t_n}\mathsf{1}_{\{T=t_n\}} \mathsf{1}_A)\\
    &=\sum_{n=1}^{\infty}\mathbb{E}\left(\mathsf{1}_{A \cap \{T = t_n\}} Z\right)\\
    &= \mathbb{E}(\mathsf{1}_A Z)
    \end{aligned}$$
    because \textcircled{1} $A \cap \{T = t_n\}\in \mathcal{F}_{t_n}$, \textcircled{2} $\mathbb{E}(Z \mid \mathcal{F}_{t_n}) = X_{t_n}$

    \textcircled{2} General T. Define $T_k = \frac{[2^k T] + 1}{2^k}.$

    To prove $X_T = \mathbb{E}(Z \mid \mathcal{F}_T)$, $\forall~ A \in \mathcal{F}_T \subseteq \mathcal{F}_{T _k}$, we get $\mathbb{E}(Z \mid \mathcal{F}_{T _k}) = X _{T _k} \implies \mathbb{E}(Z \mathsf{1}_A) = \mathbb{E}(X_{T_k} \mathsf{1}_A)$.

    Send $k \to \infty$ to see $\mathbb{E}(Z \mathsf{1}_A) = \mathbb{E}(X_T \mathsf{1}_A).$

    Because \textcircled{1} $X_{T_k} \xlongrightarrow{a.s.} X_T$ by right-continuity, \textcircled{2} $\{X_{T_k}\}$ is U.I. by $X_{T_k} = \mathbb{E}(Z \mid \mathcal{F}_{T_k})$, \textcircled{3} $\implies X_{T_k} \xlongrightarrow{L^1}X_T.$
}

\thm{Doob's Maximal Inequality}{
    Let $(X_t)_{t \geq 0}$ be a right-continuous submartingale. Then $\forall~\lambda > 0,$

    $$
        \begin{aligned}
            &\mathsf{P}(\sup_{0 \leq s \leq t} X_s > \lambda) \leq \frac{1}{\lambda}\mathbb{E}X_t^+,\\
            &\mathsf{P}(\inf_{0 \leq s \leq t} X_s < -\lambda) \leq \frac{1}{\lambda}(\mathbb{E}X_t^+ - \mathbb{E}X_0).
        \end{aligned}
    $$
}

\pf{
    $\forall~ \lambda > 0$, $T = \inf \{t \geq 0: X_t > \lambda\},$ then
    $$
        \mathsf{P}(\sup_{0 \leq s \leq t}X_s > \lambda) = \mathsf{P}(T \leq t).
    $$
    
    Consider $\{X_{t \wedge T}, t \geq 0\}$,
    $$
        \begin{aligned}
        \lambda \mathsf{P}(T \leq t) = \lambda \mathbb{E}(\mathsf{1}_{T \leq t}) &\leq \mathbb{E}(X_T \mathsf{1}_{T \leq t})\\
        & = \mathbb{E}(X_{T \wedge t} \mathsf{1}_{T \leq t}) \\
        &= \mathbb{E}(X_{T \wedge t}^+ \mathsf{1}_{T \leq t})\\
        &= \mathbb{E}(X_{T \wedge t}^+) \leq \mathbb{E}(X_t^+).
        \end{aligned}
    $$

    Define $S = \inf\{t \geq 0 : X_t < -\lambda\}$, then
    $$
        \mathsf{P}(\inf_{0 \leq s \leq t}X_s < -\lambda) = \mathsf{P}(S \leq t).
    $$
    and
    $$
        \lambda \mathsf{P}(S \leq t) = \lambda \mathbb{E}(\mathsf{1}_{S \leq t}).
    $$
    
    Because $X_S \leq - \lambda$, $\lambda \leq - X_S$, so
    $$
        \lambda \mathsf{P}(S \leq t) \leq \mathbb{E}(-X_S \mathsf{1}_{S \leq t}) = - \mathbb{E}(X_{S \wedge t} \mathsf{1}_{S \leq t}).
    $$

    By submartingale optional stopping
    $$
    \begin{aligned}
    \mathbb{E}X_0 \leq \mathbb{E}X_{t \wedge S}&= \mathbb{E}(X_{t \wedge S}\mathsf{1}_{S \leq t}) + \mathbb{E}(X_{t \wedge S}\mathsf{1}_{S > t})\\
    &\leq - \lambda \mathsf{P}(S \leq t) + \mathbb{E}(X_{t \wedge S}^+) \\
    & \leq -\lambda \mathsf{P}(S \leq t)+ \mathbb{E}(X_t^+).
    \end{aligned}
    $$
}

\thmnn{$\forall~p > 1$, $\displaystyle\mathbb{E}\left(\sup_{s \leq t}\abs{X_s} \right)^p \leq \left(\frac{p}{p-1}\right)^p \mathbb{E}\abs{X_t}^p.$}

\pf{
    Define $Y= \sup_{0 \leq s \leq t}\abs{X_s}.$
    $$
    \begin{aligned}
    \lambda \mathsf{P}(Y \geq \lambda) &\leq \mathbb{E}(\abs{X_{T \wedge t}}\mathsf{1}_{T \leq t}) \\
    & = \mathbb{E}\left( \abs{X_{T \wedge t}} \right) - \mathbb{E}\left( \abs{X_{T \wedge t}} \mathsf{1}_{T > t} \right)\\
    & \leq \mathbb{E}\left( \abs{X_t} \right) - \mathbb{E}\left( \abs{X_t}\mathsf{1}_{T >t} \right)\\
    &= \mathbb{E} \abs{X_t} - \mathbb{E}\left( \abs{X_t} \mathsf{1}_{Y < \lambda} \right)\\
    &= \mathbb{E}\left( \abs{X_t}\mathsf{1}_{Y \geq \lambda} \right).
    \end{aligned}
    $$
    Hence $\mathsf{P}(Y \geq \lambda) \leq \frac{1}{\lambda}\mathbb{E}\left( \abs{X_t}\mathsf{1}_{Y \geq \lambda} \right).$
    $$
    \begin{aligned}
        \mathbb{E}Y^p &= \int_{0}^{\infty} p \lambda^{p-1}\mathsf{P}(Y \geq \lambda) ~d \lambda \\
        &\leq \int_{0}^{\infty} p \lambda^{p-2}\mathbb{E}\left( \abs{X_t}\mathsf{1}_{Y \geq \lambda} \right)~d \lambda\\
        &=\mathbb{E}\left( \abs{X_t} \int_{0}^{\infty} p \lambda^{p-2}\mathsf{1}_{Y \geq \lambda} ~d\lambda \right)\\
        &=\mathbb{E}\left( \abs{X_t}\int_{0}^{Y}p \lambda^{p-2}~d\lambda \right)\\
        &= \mathbb{E}\left(\abs{X_t}\frac{p}{p-1}Y^{p-1}\right)\\
        &\leq \frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}\left( \mathbb{E}(Y^{p-1})^q \right)^{\frac{1}{q}}\\
        &=\frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}\left( \mathbb{E}Y^p \right)^{\frac{1}{q}}.
    \end{aligned}
    $$
    $\implies \left( \mathbb{E}Y^p \right)^{\frac{1}{p}} \leq \frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}.$

    Consider $Y \wedge k$, $\forall~ k > 0$, 
    $$
    \begin{aligned}
        \mathbb{E}\left( Y \wedge k \right)^p &= \int_{0}^{\infty} p \lambda^{p-1}\mathsf{P}(Y\wedge k \geq \lambda)~d \lambda \\
        &= \int_{0}^{k}p \lambda^{p-1}\mathsf{P}(Y \wedge k \geq \lambda)~d \lambda + \underbrace{\int_{k}^{\infty} p \lambda^{p-1}\mathsf{P}(Y\wedge k \geq \lambda)~d \lambda}_{=0} \\
        &= \int_{0}^{k}p \lambda^{p-1}\mathsf{P}(Y \wedge k \geq \lambda)~d\lambda\\
        &= \int_{0}^{k}p \lambda^{p-1}\mathsf{P}(Y \geq \lambda) ~d \lambda\\
        &\leq \mathbb{E}\left( \abs{X_t}
        \int_{0}^{k} p \lambda^{p-1} \mathsf{1}_{Y \geq \lambda}~d \lambda \right)\\
        &= \mathbb{E}\left( \abs{X_t}\int_{0}^{k \wedge Y} p \lambda^{p-2}~d \lambda \right)\\
        &= \mathbb{E}\left[ 
            \abs{X_t} \frac{p}{p-1}(Y \wedge k )^{p-1}
         \right].
    \end{aligned}
    $$

    $\implies \mathbb{E}(Y \wedge k)^p \leq \frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}\left( 
        \mathbb{E}(Y \wedge k)^p
     \right)^{\frac{1}{q}}.$

    $\implies \left( \mathbb{E}(Y \wedge k)^p \right)^{\frac{1}{p}} \leq \frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}$.

    Let $k\to \infty$, we get
    $$
        \left( \mathbb{E} Y^p \right)^{\frac{1}{p}} \leq \frac{p}{p-1}\left( \mathbb{E}\abs{X_t}^p \right)^{\frac{1}{p}}.
    $$
}

\subsection{The Doob-Meyer Decomposition}
Recall the Doob Decomposition
$$
    X_n = M_n + A_n,
$$
where $X_n$ is a submartingale, $M_n$ is a martingale, and $A_n$ is a predictable increasing process.

$$
    \begin{aligned}
        &\mathbb{E}\left( M_{n+1} \mid \mathcal{F}_n \right) = M_n \\
        \implies& \mathbb{E}(X_{n+1}- A_{n+1}\mid \mathcal{F}_n) = X_n - A_n \\
        \implies& \mathbb{E}(X_{n+1}\mid \mathcal{F}_n) - A_{n+1} = X_n - A_n \\
        \implies& A_{n+1} - A_n = \mathbb{E}(X_{n+1}\mid \mathcal{F}_n) - X_ n \geq 0.
    \end{aligned}
$$

$$
\begin{cases}
A_n = \sum_{k=0}^{n-1}\left( \mathbb{E}(X_{k+1} \mid \mathcal{F}_k) - X_k \right)\\
A_0 = 0.
\end{cases}
$$

Next, for a right-continuous submartingale $(X_t)_{t \geq 0}$, $X_t = M_t + A_t$, where $M_t$ is a martingale, $A_t$ is a increasing \& ``predictable''.

Consider $(A_t)_{t \geq 0}$ adapted.

\defnn{
        An adapted process $A$ is called increasing if a.s.
    \begin{enumerate}[label=(\alph*)]
        \item $A_0 = 0$;
        \item $t \mapsto A_t$ is nondecreasing and right-continuous;
        \item $\mathbb{E}(A_t) < \infty$, $\forall~ t \geq 0 $. 
    \end{enumerate}
    Integrable: $\mathbb{E}(A_\infty) = \mathbb{E}(\lim_{t \to \infty}A_t) < \infty.$
}

\defnn{
    An increasing process $A$ is called natural if $\forall~$bounded, right-continuous martingale $(M_t)_{t \geq 0}$, we have $\forall~ t > 0$,
    $$
    \mathbb{E}\int_{0}^{t} M_s ~d A_s = \mathbb{E} \int_{0}^{t} M_{s-} ~d A_s.
    $$
}
\rmkb{If $s \mapsto A_s$ is a.s. continuous, then $A$ is natural.}

\defnn{
    An increasing sequence ($A_n,\mathcal{F}_n$) is called natural if $\forall~$bounded martingale $\{M_n,\mathcal{F}_n\}$,
    $$
    \mathbb{E}(M_n A_n) = \mathbb{E}\left( \sum_{k=1}^n M_{k-1}(A_k - A_{k-1}) \right).
    $$
}

\prop{
    A predictable $\Longleftrightarrow$ A natural.
}

\pf{
    ``$\Rightarrow~$'' To prove
    $$
    \begin{aligned}
        &\mathbb{E}(M_n A_n) = \sum_{k=1}^n \mathbb{E}M_{k-1}(A_k - A_{k-1}) - \sum_{k=1}^n(M_{k-1} A_k) - \mathbb{E}(M_{k-1}A_{k-1})\\
        \Longleftrightarrow& \sum_{k=2}^n \mathbb{E}(M_{k-1}A_{k-1}) + \mathbb{E}(M_n A_n) = \sum_{k=1}^n \mathbb{E}(M_{k-1}A_k)\\
        \Longleftrightarrow &
        \sum_{k=1}^n \mathbb{E}(M_k A_k) = \sum_{k=1}^n \mathbb{E}(M_{k-1}A_k)\\
        \Longleftrightarrow &
        \sum_{k=1}^n \mathbb{E}\left[ 
            (M_k - M_{k-1})A_{k} = 0.
         \right]
    \end{aligned}
    $$

    So if $A_n \in \mathcal{F}_{n-1}$, 
    $$
    \mathbb{E}\left( 
        A_n (M_n - M_{n-1})
     \right) = \mathbb{E} \left[ 
        \underbrace{\mathbb{E}(M_n - M_{n-1} \mid \mathcal{F}_n )}_{=0}A_n 
      \right]=0.
    $$

    ``$\Leftarrow~$'' Assume $A$ is natural. Then $\forall~$bounded $(M_n)$,
    $$
    \mathbb{E}\left[ 
        A_n (M_n - M_{n-1})
     \right] = 0,\quad \forall~ n \geq 1.
    $$

    \textbf{Goal:} $A_n \in \mathcal{F}_{n-1} \Longleftrightarrow A_n = \mathbb{E}(A_n \mid \mathcal{F}_{n-1})$ a.s..
    $$
    \begin{aligned}
        &\mathbb{E}\left[ 
            M_n\left( A_n - \mathbb{E}(A_n \mid \mathcal{F}_n) \right)
         \right]\\
         =& \mathbb{E}\left( A_n M_{n-1} \right) - \mathbb{E} \left[ 
            M_n \mathbb{E}(A_n \mid \mathcal{F}_{n-1})
          \right]\\
         =& \mathbb{E}\left( M_{n-1} \left( A_n - \mathbb{E}(A_n \mid \mathcal{F}_{n-1}) \right) \right) + \mathbb{E}\left[ 
            \mathbb{E}(A_n \mid \mathcal{F}_{n-1}) (M_n - M_{n-1})
          \right]\\
         =&\mathbb{E}(M_{n-1}A_n) - \mathbb{E}\left[ \mathbb{E}(A_nM_{n-1}\mid \mathcal{F}_{n-1}) \right] + \mathbb{E}\left[ 
            \mathbb{E}(A_n \mid \mathcal{F}_{n-1}) \mathbb{E}(M_n - M_{n-1} \mid \mathcal{F}_{n-1})
          \right]= 0.
    \end{aligned}
    $$

    Fix $n \geq 1$. Let 
    $$
        X = \mathrm{sgn} \left( A_n - \mathbb{E}(A_n \mid \mathcal{F}_{n-1}) \right)
    $$
    Then $X\left( A_n - \mathbb{E}(A_n\mid \mathcal{F}_{n-1}) \right) = \abs{A_n - \mathbb{E}(A_n \mid \mathcal{F}_{n-1})}$.

    Define 
    $$ M_k = \begin{cases}
       \mathbb{E}(X \mid \mathcal{F}_{k}),& 0 \leq k \leq n-1\\
       X, &k=n
    \end{cases}$$

    Check: $\mathbb{E}(M_{k+1} \mid \mathcal{F}_{k}) = M_k$, $k \geq n$, $\mathbb{E}(X \mid \mathcal{F}_{k}) = X$.

    $$
    \begin{aligned}
        0 &= \mathbb{E}\left[ M_n (A_n - \mathbb{E}(A_n \mid \mathcal{F}_{n-1})) \right]\\
        &= \mathbb{E} \abs{A_n - \mathbb{E}(A_n \mid \mathcal{F}_{n-1})}
    \end{aligned}
    $$
    $\implies A_n = \mathbb{E}(A_n \mid \mathcal{F}_{n-1})$ a.s.
    
    $\implies A_n \in \mathcal{F}_{n-1}.$
}

$$
    \begin{aligned}
        &\mathbb{E}(M_n A_n) = \sum_{k=1}^n \mathbb{E}\left( M_{k-1} (A_k - A_{k-1}) \right) \\
        \Longleftrightarrow &
        \mathbb{E}(M_tA_t) = \mathbb{E}\left( \int_{0}^{t}M_{s-}~dA_s \right) = \mathbb{E}\left( \int_{0}^{t} M_s ~dA_s \right).
    \end{aligned}
$$

$$
    \int_{0}^{t} M_s ~d A_s = \lim_{\norm{\Pi}\to 0} \int_{0}^{t} M_s^\Pi ~d A_s.
$$
where $\Pi = \{t_0, t_1,\cdots,t_n\}\subseteq[0,t]$ with $0 = t_0 < t_1 < \cdots < t_n = t$ and $\norm{\Pi} = \max_{1 \leq k \leq n}(t_k - t_{k-1}).$ $M_s \xlongleftarrow{a.s.} M_s^\Pi = \sum_{k=1}^{n} M_{t_k}\mathsf{1}_{(t_{k-1},t_k]}(s)$ by right-continuity. For each $\Pi$, 
$$
\begin{aligned}
&\mathbb{E}\int_{0}^{t}M_s ^\Pi ~d A_s \\
=& \mathbb{E} \sum_{k=1}^n M_{t_k}(A_{t_k} - A_{t_{k-1}}) \\
=& \sum_{k=1}^n \mathbb{E}(M_{t_k A_{t_k}}) - \mathbb{E}(M_{t_k}A_{t_{k-1}})\\
=&\mathbb{E}(M_{t_n}A_{t_n}) + \sum_{k=1}^{n-1}\mathbb{E}(M_{t_{k}} A_{t_{k}}) - \sum_{k=2}^{n}\mathbb{E}(M_{t_k} A_{t_{k-1}})\\
=& \mathbb{E}(M_t A_t) + \sum_{k=1}^{n-1}\mathbb{E}(M_{t_k} A_{t_k}) - \sum_{k=1}^{n-1}\mathbb{E}(M_{t_{k+1}}A_{t_k}) \\
=& \mathbb{E}(M_t A_t) - \underbrace{\sum_{k=1}^{n}\mathbb{E}\left( A_{t_k} (M_{t_{k+1}} - M_{t_k}) \right)}_{=0}
\end{aligned}
$$

\defnn{
    Let $S = \{\text{stopping time }T \text{ with }T < \infty \text{ a.s.}\}$. We say a right-continuous $\{X_t\}_{t \geq 0}$ is of class $D$ if $\{X_T\}_{T \in S}$ is U.I. $\implies$ implies $\{X_t\}_{t \geq 0}$ is U.I..

    Fix $a > 0$. Let $S_a = \{\text{stopping time } T \text{ with }T \leq a \text{ a.s.}\}.$ $\{X_t\}_{t \geq 0}$ is of class $DL$ if $\{X_T\}_{T \in S_a}$ is U.I. for every $a > 0$.
}


\thm{Doob-Meyer Decomposition}{
    If $\{X_t\}$ is of class $DL$, then $X_t = M_t + A_t$ with $\{M_t\}$ is martingale, $\{A_t\}$ is increasing.

    If $(A_t)$ is natural, then $(M_t, A_t)$ is unique.
}

\pf{
    Assume $X_t = \begin{cases}
        M_t + A_t \\M_t' + A_t'
    \end{cases}$ with $M_t,M_t'$ martingale, $A_t, A_t'$ natural.

    Let $B_t = M_t - M_t' = A_t' - A_t$ is a martingale and of bounded variation \underline{natural}.
    
    $\forall~$bounded continuous martingale $\{\xi_t\}_{t \geq 0}$, 
    $$
    \begin{aligned}
        \mathbb{E} \xi_t (A_t - A_t')& = \mathbb{E}\int_{0}^{t}\xi_{s-} (d A_s - d A_s') \\
        &= \mathbb{E}\left[ \int_{0}^{t}\xi_{s-}  dB_s \right]\\
        &=\lim_{n \to \infty} \mathbb{E}\sum_{j=1}^{m_n}\xi_{t_{j-1}^{(n)}} \left[ B_{t_j^{(n)}} - B_{t_{j-1}^{(n)}} \right]
    \end{aligned}
    $$ 
    where $\Pi_n = \{t_0^{(n)}, \cdots, t_{m_n}^{(n)}\}$ is a partition of $[0,t]$.

    $$
    \mathbb{E}\left[ 
        \xi_{t_{j-1}^{(n)}} (B_{t_j^{(n)}} - B_{t_{j-1}^{(n)}})
     \right] = \mathbb{E} \left[ 
        \xi_{t_{j-1}^{(n)}} \underbrace{\mathbb{E}(B_{t_j^{(n)}} - B_{t_{j-1}^{(n)}} \mid \mathcal{F}_{t_{j-1}^{(n)}})}_{=0}
      \right]
    $$
    Hence $\mathbb{E}\left[ \xi_t (A_t - A_t')  \right] = 0.$

    $\forall~$bounded variable $\xi$, we let $\xi_t = \mathbb{E}(\xi \mid \mathcal{F}_t)$. Then $\mathbb{E}\left[ \mathbb{E}(\xi \mid \mathcal{F}_t)(A_t - A_t') \right] = 0 \Rightarrow~ \mathbb{E}\left[ \xi (A_t - A_t') \right] = 0$.
    
    $\xi = \mathrm{sgn}(A_t - A_t') \Rightarrow~ \mathbb{E}\abs{A_t - A_t'} = 0 \Rightarrow~A_t = A_t'$, a.s. $\forall~ t > 0 \Rightarrow~ A_t = A_t'$, $\forall~ q \in \mathbb{Q}_+$ a.s. $\Rightarrow~$Right-continuity, $A_t = A_t'$, $\forall~ t \geq 0$ a.s..

    \textbf{Existence: }Fix $a > 0$.
    
    We only need to prove $X_t = M_t + A_t$, $\forall~ 0 \leq t \leq a$.

    Consider $Y_t \triangleq X_t - \mathbb{E}(X_a \mid \mathcal{F}_t)$. $(Y_t)_{t \geq 0}$ is a submartingale because 
    $$
    \begin{aligned}
    \mathbb{E}(Y_t \mid \mathcal{F}_s)&= \mathbb{E}(X_t \mid \mathcal{F}_s) - \mathbb{E}(X_a \mid \mathcal{F}_s) \\
    &\geq X_s - \mathbb{E}(X_a \mid \mathcal{F}_s) = Y_s, \quad \forall~t \geq s.
    \end{aligned}
    $$

    $\Pi_n = \{t_0^{(n)},\cdots, t_{2^n}^{(n)}\}$ with $t_j^{(n)} = \frac{j}{2^n}a$, $0 \leq j \leq 2^n$. Then $\forall~ 0 \leq j \leq 2^n$, 
    $$
        Y_{t_{j}^{(n)}} = M_{t_{j}^{(n)}}^{(n)} + A_{t_j^{(n)}}^{(n)}
    $$
    with $A_{t_j^{(n)}}^{(n)} = \sum_{k=0}^{j-1}\mathbb{E}\left( Y_{t_{k+1}^{(n)}} - Y_{t_{k}^{(n)}} \middle| \mathcal{F}_{t_{k}^{(n)}}\right)$
    $$
    \begin{cases}
        Y_0 = X_0 - \mathbb{E}(X_a \mid \mathcal{F_0}) \leq 0\\
        Y_a = X_a - \mathbb{E}(X_a \mid \mathcal{F}_a) = 0.
    \end{cases}
    $$
    $0 = M_a ^{(n)} - A_a^{(n)}$. Hence $M_{t_{j}^{(n)}}^{(n)} = \mathbb{E}(M_a^{(n)} \mid \mathcal{F}_{t_{j}^{(n)}}) = - \mathbb{E}(A_a^{(n)} \mid \mathcal{F}_{t_{j}^{(n)}}).$

    $\implies Y_{t_{j}^{(n)}} = A_{t_{j}^{(n)}}^{(n)} - \mathbb{E}(A_a^{(n)} \mid \mathcal{F}_{t_{j}^{(n)}})$.

    We will show $\{A_a^{(n)}\}_{n \geq 1}$ is U.I.. Then by Dunford-Pettis compactness criterion, $\exists~$integrable $A_a$ s.t. 
    $$
        A_a^{(n_k)} \xlongrightarrow{L^1} A_a
    $$ 
    i.e. $\forall~$bounded r.v. $\xi$, 
    $$
    \lim_{k \to \infty} \mathbb{E}(\xi A_a^{(n_k)}) = \mathbb{E}(\xi A_a).
    $$

    Define $A_t = Y_t + \mathbb{E}(A_a \mid \mathcal{F}_t)$.

    4.11 Problem $\Rightarrow~$If $\mathbb{E}(\xi A_a^{(n_k)}) \to \mathbb{E}(\xi A_a)$ then 
    $$
    \mathbb{E}(\xi \mathbb{E}(A_a^{(n_k)} \mid \mathcal{F}_t)) \to \mathbb{E}(\xi \mathbb{E}(A_a \mid \mathcal{F}_t))
    $$

    $A_a^{(n_k)} \xlongrightarrow{L^1} A_a \Rightarrow~ \mathbb{E}(A_a^{(n_k)} \mid \mathcal{F}_t) \xlongrightarrow{L^1} \mathbb{E}(A_a \mid \mathcal{F}_t)$.

    $$\begin{cases}
        A_t = Y_t + \mathbb{E}(A_a \mid \mathcal{F}_t) \\
        A_t^{(n)} = Y_t + \mathbb{E}(A_a^{(n)} \mid \mathcal{F}_t)
    \end{cases}$$ 
    for any $t = t_{j}^{(n)}$.

    Hence $\mathbb{E}(\xi A_t^{(n)}) \to \mathbb{E}(\xi A_t)$. 

    For any $0 \leq s < t \leq a$ with $s,t \in \{\frac{j}{2^n}, n \geq 1, 0 \leq j \leq 2^n\}$,
    $$
        \mathbb{E}(\xi (A_t - A_s)) = \lim_{k \to \infty}\mathbb{E}(\xi (A_t^{(n_k)} - A_s^{(n_k)}) ) \geq 0 \quad \text{if } \xi \geq 0.
    $$

    By letting $\xi = \mathsf{1}_{A_s > A_t}$, $\mathbb{E}\left[ (A_t - A_s) \mathsf{1}_{A_s - A_t} \right] \geq 0.$

    $\Rightarrow~ A_t - A_s \geq 0$ a.s.

    $A_0^{(n)} = 0 = Y_0 + \mathbb{E}(A_a^{(n)} \mid \mathcal{F}_0) \to Y_0 + \mathbb{E}(A_a \mid \mathcal{F}_0) = A_0$.

    $\Rightarrow~ A_0 = 0$.

    $X_t - \mathbb{E}(X_a \mid \mathcal{F}_t) =Y_t = A_t^{(n)}- \mathbb{E}(A_a^{(n)} \mid \mathcal{F}_t).$

    $\Rightarrow~ Y_t = A_t - \mathbb{E}(A_a \mid \mathcal{F}_t) = X_t-\mathbb{E}(X_a \mid \mathcal{F}_t).$

    $\Rightarrow~ X_t = A_t + \mathbb{E}(X_a - A_a \mid \mathcal{F}_t) = A_t + M_t$.

    To prove $\{A_t\}$ is natural,
    $$\mathbb{E}\int_{0}^{a} \xi_s ~dA_s = \mathbb{E}\int_{0}^{a}\xi_{s-}~dA_s.$$
    $$LHS = \mathbb{E}(\xi_a A_a) = \lim_{n \to \infty}\mathbb{E}(\xi_a A_a^{(n)})$$

    $$
    RHS = \lim_{n \to \infty} \mathbb{E} \sum_{j=1}^{2^n}\xi_{t_{j-1}^{(n)}}(A_{t_{j}^{(n)}} - A_{t_{j-1}^{(n)}}).
    $$

    Because $\{A_{t_{j}^{(n)}}^{(n)}\}_{0 \leq j \leq 2^n}$ is predictable,
    $$
    \begin{aligned}
    \mathbb{E}(\xi_a A_a^{(n)}) &= \mathbb{E}\left( \sum_{j=1}^{2^n}\xi_{t_{j-1}^{(n)}} (A_{t_{j}^{(n)}}^{(n)} - A_{t_{j-1}^{(n)}}^{(n)}) \right)\\
    &=\mathbb{E}\left( \sum_{j=1}^{2^n}\xi_{t_{j-1}^{(n)}}(Y_{t_{j}^{(n)}}-Y_{t_{j-1}^{(n)}}) \right)\\
    &=\mathbb{E}\left( \sum_{j=1}^{2^n}\xi_{t_{j-1}^{(n)}}(A_{t_{j}^{(n)}}-A_{t_{j-1}^{(n)}}) \right)
    \end{aligned}
    $$
}

\section{Square Integrable}
\defnn{
    Let $(X_t)_{t \geq 0}$ be a right-continuous martingale. We say $X$ is square-integrable if
    $$
    \mathbb{E}X_t^2 < \infty, \quad \forall~t \geq 0.
    $$
    Assume $X_0 = 0$, $X \in M_2.$
}

$Y_t = X_t^2$ is a submartingale.

Recall \underline{DL}: $\forall~$stopping time $T \leq a$, $\{Y_T\}_{T}$ is U.I..

By Doob-meyer decomposition, $Y_t = X_t^2 = M_t + A_t$, $\forall~ t \geq 0.$

If $X \in M_2^c$ (continuous), we may let $M_t$ and $A_t$ be continuous.

\defnn{
    $\forall~ X \in M_2$, the quadratic variation of $X$ is defined by $\langle X \rangle_t \triangleq A_t$ i.e. $\langle X \rangle_{t}$ is the unique adapted, natural increasing process such that $\langle X \rangle_0 = 0$ and $X_t^2 - \langle X \rangle_{t}$ is a martingale.
}

If we take $X,Y \in M_2$, then 
$$
\begin{cases}
    (X_t + Y_t)^2 - \langle X+Y \rangle_t \text{ is mart,}\\
    (X_t - Y_t)^2 - \langle X-Y \rangle_{t} \text{ is mart.} 
\end{cases}
$$
$\implies 4X_tY_t - \left( \langle X+Y \rangle_t - \langle X-Y \rangle_t   \right)$ is a mart.
\defnn{
    For $X,Y \in M_2$, we define the cross variation $$\langle X,Y \rangle_t \triangleq \frac{1}{4}\left( \langle X+Y \rangle_t - \langle X-Y \rangle_t   \right) $$
    such that $X_tY_t - \langle X,Y \rangle_t $ is a martingale.
}
\rmkb{
    $\langle X,X \rangle_t = \langle X \rangle_t.$
}
\defnn{
    The p-th variation of $X$ over the partition $\Pi = \{t_0,\cdots,t_m\}$ of $[0,t]$ is
    $$
    V_t^{(p)}(\Pi) = \sum_{k=1}^{m}\abs{ X_{t_k} - X_{t_{k-1}} }^p.
    $$
    when $p = 2$, we call $V_t^{(2)}(\Pi)$ the quadratic variation.
}
\thmnn{
    Let $X \in \mathcal{M}_2^c$. For partition $\Pi$ of $[0,t]$, we have $V_t^{(2)}(\Pi) \xlongrightarrow{P} \langle X \rangle_t.$
}
\pf{
    $$
    \begin{aligned}
    &\mathbb{E}\left( V_t^{(2)}(\Pi) - \langle X \rangle_t  \right)^2 \\
    =& \mathbb{E}\left[ 
        \sum_{k=1}^{m}\abs{ X_{t_{k}} -X_{t_{k-1}}}^2 - \langle X \rangle _t
     \right]^2\\
     =& \mathbb{E}\left[ 
        \sum_{k=1}^{m}\left( 
            \abs{ X_{t_{k}} -X_{t_{k-1}}  }^2 - \left( \langle X \rangle_{t_k} - \langle X \rangle_{t_{k-1}}   \right)
         \right)
      \right]^2 \\
      =& \sum_{k=1}^{m} \mathbb{E} \left[ 
        \left( X_{t_{k}} -X_{t_{k-1}}  \right)^2 - \left( \langle X \rangle_{t_k} - \langle X \rangle_{t_{k-1}}   \right)
       \right]^2 \\
       &+ 2 \sum_{1 \leq j < \leq m}\mathbb{E}\left[ 
        \left( X_{t_{k}} -X_{t_{k-1}}  \right)^2 - \left( \langle X \rangle_{t_k} - \langle X \rangle_{t_{k-1}}   \right)
        \right]\left[ \left( 
            X_{t_{j}} - X_{t_{j-1}}
         \right)^2 - \left( \langle X \rangle_{t_j} - \langle X \rangle_{t_{j-1}}   \right) \right]. (\textbf{H})
    \end{aligned}$$
    Notice that
    $$
    \mathbb{E}\left[ 
        \left( X_{t_{k}} - X_{t_{k-1}} \right)^2 \middle| \mathcal{F}_{t_j} 
     \right] = \mathbb{E}\left[ 
        X_{t_{k}}^2 \middle| \mathcal{F}_{t_{j}}
      \right] - 2 \mathbb{E}\left( X_{t_{k}}X_{t_{k-1}} 
        \middle| \mathcal{F}_{t_j} \right) + \mathbb{E}\left( X_{t_{k-1}}^2 \middle| \mathcal{F}_{t_j} \right).
    $$
    For the cross-term,
    $$
    \begin{aligned}
        \mathbb{E}\left( X_{t_{k}}X_{t_{k-1}}\middle| \mathcal{F}_{t_{j}} \right) = \mathbb{E}\left[ 
            \mathbb{E}\left( X_{t_{k}}X_{t_{k-1}}\middle| \mathcal{F}_{t_{k-1}} \right)\middle| \mathcal{F}_{t_j}
         \right]= \mathbb{E}\left[ X_{t_{k-1}}\mathbb{E}\left( X_{t_{k}} \middle| \mathcal{F}_{t_{k-1}} \right) \middle| \mathcal{F}_{t_j} \right].
    \end{aligned}
    $$
    Hence 
    $$
    \mathbb{E}\left[ \left( X_{t_{k}} -X_{t_{k-1}} \right)^2 \middle| \mathcal{F}_{t_j} \right] = \mathbb{E} \left[ 
        X_{t_{k}}^2 - X_{t_{k-1}}^2 \middle| \mathcal{F}_{t_j}
     \right] = \mathbb{E}\left[ \langle X \rangle_{t_k} - \langle X \rangle_{t_{k-1}} \middle| \mathcal{F}_{t_j}   \right]
    $$
    Then
    $$
    \begin{aligned}
        (\textbf{H}) &\leq \sum_{k=1}^{m}\left[ 
            2 \mathbb{E} \left( X_{t_{k}} - X_{t_{k-1}} \right)^4 + 2 \mathbb{E} \left( \langle X \rangle_{t_k} - \langle X \rangle_{t_{k-1}}   \right)^2
         \right] + 2 \mathbb{E}\left[ \sup_{1 \leq k \leq m} \abs{ \langle X \rangle_{t_{k}} - \langle X \rangle_{t_{k-1}}   } \langle X \rangle_{t}  \right]\\
         & = I + II.
    \end{aligned}
    $$
    Note $I = 2 \mathbb{E} V_t^{(4)}(\Pi)$.
    \lemnn{
        Let $X \in \mathcal{M}_2$ satisfy $\abs{ X_s } \leq K< \infty,$ $\forall~ s \leq t$. Then $\mathbb{E}\left[ V_t^{(2)}(\Pi)^2 \right]\leq 6K^4$
    }
    Assuming this, then
    $$
    \mathbb{E}V_t^{(4)}(\Pi) \leq \mathbb{E}\left( 
        \sum_{k=1}^{m}\abs{ X_{t_{k}}-X_{t_{k-1}} }^2 \cdot m_t^2\left( X;\norm{ \Pi } \right)
     \right)
    $$
    where $m_t(X,\delta) = \sup\{\abs{ X_r - X_s }
    \colon \abs{ s-r }\leq \delta,\, 0 \leq r < s \leq t\}.$
    By Cauchy-Schwartz, 
    $$
    \begin{aligned}
    &\mathbb{E}\left[ V_t^{(4)}(\Pi) \right]\\
    \leq& \mathbb{E} \left[ V_t^{(2)}(\Pi)\cdot m_t^2(X, \norm{ \Pi }) \right]\\
    \leq&\left( \mathbb{E}\left[ V_t^{(2)}(\Pi)^2 \right] \right)^{\frac{1}{2}}\left( \mathbb{E}m_t^4(X;\norm{ \Pi }) \right)^{\frac{1}{2}}\\
    \leq&(6K^4)^\frac{1}{2}\cdot \left( \mathbb{E}\left[ m_t^4 \left( X,\norm{ \Pi } \right) \right] \right)^{\frac{1}{2}}.
    \end{aligned}
    $$  

    If $\abs{ X_s } \leq K < \infty$, $\forall~ 0 \leq s \leq t$, by bounded convergence $\mathbb{E}\left[ m_t^4(X,\norm{ \Pi }) \right] \to 0$ as $\norm{ \Pi } \to 0.$

    \lemnn{
        Let $X \in \mathcal{M}_2$ satisfy $\abs{ X_s } \leq K < \infty$, $\forall~ 0 \leq s \leq t$. Then
        $$
            \mathbb{E}\left[ V_t^{(4)}(\Pi) \right] \to 0 \quad \text{a.s. }\norm{ \Pi } \to 0.
        $$
    }
    For II. Let $X \in \mathcal{M}_2^c$ satisfy $\langle X \rangle_{s}  \leq K < \infty$, $\forall~ 0 \leq s \leq t$. Then 
    $$
    \mathbb{E}\left[ 
        \langle X \rangle_{t} \cdot \sup_{1 \leq k \leq m} \abs{  \langle X \rangle_{t_{k}} - \langle X \rangle_{t_{k-1}}   } 
     \right] \to 0.
    $$
    [proof: By uniformly Continuity]

    \textbf{Summary:} If $\abs{ X_s } \leq K < \infty$ and $\langle X \rangle_{s} \leq K $, $\forall~ 0 \leq s \leq t$, then 
    $$
    \mathbb{E}\left[ 
        \left( V_{t}^{(2)}(\Pi) - \langle X \rangle_{t}  \right)^2
        \right] \to 0 \quad \text{a.s. } \norm{ \Pi } \to 0.
    $$

    Next, for general $X \in \mathcal{M}_2^c$, we use ``Localization'' to define $\forall~n \geq 1$,
    $$
    T_n = \inf \{ t \geq 0 \colon \abs{ X_t }\geq n \text{ or } \langle X \rangle_{t} \geq n \}.
    $$

    Then $X_t^{(n)} = X_{t \wedge T_n}$ is a bounded martingale. Also $\left\{ X_{t \wedge T_n}^2 - \langle X \rangle_{t \wedge T_n}  \right\}_{t \geq 0}$ is a bounded mart.

    By uniqueness of $\langle X^{(n)} \rangle_{t} $, we get $\langle X^{(n)} \rangle_{t} = \langle X \rangle_{t \wedge T_n}  $ and $(X_t^{(n)})^2 - \langle X^{(n)} \rangle_{t} $ is mart.

    Hence $\forall~ 0 \leq s \leq t$
    $$
    \begin{cases}
        \abs{ X_s^{(n)} } \leq n < \infty, \\
        \langle X^{(n)} \rangle_{s} \leq n < \infty. 
    \end{cases}
    $$
    $$
    \implies \mathbb{E}\left[ 
        \sum_{k=1}^{m}\left( X_{t_{k} \wedge T_n} -X_{t_{k-1} \wedge T_n}  \right)^2 - \langle X \rangle_{t \wedge T_n}
        \right]^2 \to 0 \quad \text{a.s. } \norm{ \Pi } \to 0.
    $$

    $T_n \uparrow \infty$ a.s.
    $\forall~$fixed $t >0$, $\mathsf{P}(T_n < t)\to 0$. We need to prove $\forall~\varepsilon > 0$, $\forall~\eta>0$, $\exists~\delta > 0$, if $\norm{ \Pi } \leq \delta,$ then 
    $$
    \mathsf{P}\left( \abs{V_t^{(2)}(x) - \langle X \rangle_{t} }  > \varepsilon \right) < \eta.
    $$

    Let $n \geq 1$ be large such that $\mathsf{P}(T_n < t) \leq \frac{\eta}{2}$. Then 
    $$  
    \begin{aligned}
    \mathsf{P}\left( \abs{V_t^{(2)}(X) - \langle X \rangle_{t} }  > \varepsilon \right) &= \mathsf{P}(T_n < t) + \mathsf{P}\left(  T_n \geq t, \abs{ V_{t \wedge T_n}^{(2)}(X)-\langle X \rangle_{t \wedge T_n}  } > \varepsilon\right)\\
    &\leq \frac{\eta}{2} + \frac{1}{\varepsilon^2}\mathbb{E}\left[ 
        \left( 
            V_{t \wedge T_n}^{(2)}(X) - \langle X \rangle_{t \wedge T_n} 
            \right)^2
        \right]\quad (\textbf{Q}).
    \end{aligned}
    $$
    Let $\norm{  \Pi }$ be small such that 
    $$
    (\textbf{Q}) \leq \frac{\eta}{2} + \frac{1}{\varepsilon^2} \varepsilon^2 \frac{\eta}{2}.
    $$
    Hence $\mathsf{P}\left( \abs{V_t^{(2)}(x) - \langle X \rangle_{t} }  > \varepsilon \right) \leq \eta.$
}

\cor{
    $V_t^{(2)}(\Pi) \xlongrightarrow{P} \langle X \rangle_{t} \implies 
    \begin{cases}
    \forall~q > 2,\quad V_{t}^{(q)}(\Pi) \xlongrightarrow{P} 0,\\
    \forall~q < 2,\quad V_{t}^{(q)}(\Pi) \xlongrightarrow{P} \infty
    \end{cases} $
    on $\{\langle X \rangle_{t} > 0 \}$.
}

$\mathcal{M}_2 = \{X\colon\mathbb{E} X_t^2 <\infty,\, \forall~ t\}$.

$\mathcal{M}^2 = \{X\colon \sup_{t \geq 0} \mathbb{E}X_t^2 < \infty\}$.

$\mathcal{M}^2_0 = \{X \in M^2 \colon X_0 = 0\}$.


\section{\texorpdfstring{$L^2$}-Theory}
The space $\mathcal{M}^2 \longleftrightarrow L^2(\mathcal{F}_{\infty})$, $X \mapsto X_{\infty}$. $\forall~ X \in \mathcal{M}^2$, we have 
$$
\begin{cases}
    X_{\infty} = \lim_{t \to \infty} X_t \text{ a.s.,}\\
    X_t \xlongrightarrow{L^2} X_{\infty},
\end{cases}
$$
$\forall~ X_\infty \in L^2(\mathcal{F}_{\infty})$, we have $X_{t} = \triangleq \mathbb{E}\left( X_\infty \middle| \mathcal{F}_{t} \right) \in \mathcal{M}^2.$

$$
\mathbb{E}\left( X_t^2 \right) = \mathbb{E}\left[ \mathbb{E}\left( X_\infty \middle| \mathcal{F}_t \right)^2 \right] \leq \mathbb{E}\left[ \mathbb{E}\left( X_\infty^2 \middle| \mathcal{F}_t \right) \right] = \mathbb{E}[X_\infty^2].
$$
$\forall~ X,Y \in \mathcal{M}^2$, we define $\langle X,Y \rangle \triangleq \mathbb{E}\left[ X_{\infty}Y_{\infty} \right]. $

\thm{Doob's $L^2$ inequality}{
    $$
    \mathbb{E}\left[ \sup_{0 \leq s \leq t}\abs{ X_s }^2 \right] \leq 4 \mathbb{E}\left[ X_t^2 \right].
    $$
    Let $t \to \infty$ to see 
    $$
    \mathbb{E}\left[ \sup_{t \geq 0}\abs{ X_s }^2 \right] \leq 4 \mathbb{E}[X_\infty^2].
    $$
}

$\mathcal{M}^2$ is complete: For any Cauchy sequence $\{M^{(n)}\}$ in $\mathcal{M}^2$, we have $\{M_{\infty}^{(n)}\}$ is Cauchy in $L^2(\mathcal{F}_{\infty})$. $\implies\exists~M_{\infty} \in L^2(\mathcal{F}_{\infty})$ s.t. $M_{\infty}^{(n)} \to M_{\infty}$ in $L^2$.

Then if $M_t = \mathbb{E}(M_\infty \mid\mathcal{F}_t)$ then
$$
    \sup_{t \geq 0}\abs{ M_t^{(n)} - M_t } \to 0 \text{ in } L^2.
$$

\pf{
    $$\mathbb{E}\left[ 
        \sup_{t \geq 0}\abs{ M_t^{(n)} - M_t }^2
     \right] = \mathbb{E}\left[ 
        \sup_{t \geq 0} \mathbb{E}\left( 
            M_\infty^{(n)} - M_\infty \middle| \mathcal{F}_t
         \right)^2
      \right] \leq 4 \mathbb{E}\left[ \left( M_\infty^{(n)} - M_{\infty} \right)^2 \right] \to 0.$$
}

\cor{
    The space $c \mathcal{M}^2$ of continuous mart in $\mathcal{M}^2$ is closed in the topology of $\mathcal{M}^2$.
}

\pf{
    If $M^{(n)} \to M$ for $M^{(n)} \in c \mathcal{M}^2$, then
    $$
    \sup_{t \geq 0} \abs{ M_t^{(n)} - M_t } \to 0 \text{ in }L^2
    $$
    $\exists~(n_k)$ s.t. $\sup_{t \geq 0}\abs{ M_t^{(n_k)} - M_k }\to 0$ a.s.
    
    $\implies (M_t)$ is continous. 
}

\defn{Weakly orthogonal}{
    $\forall~M,N \in \mathcal{M}_0^2$, if $\mathbb{E}[M_\infty N_\infty] = 0$.
}

\defn{Strongly Orthogonal}{
    $\forall~$stopping time $T$, $\mathbb{E}[M_T N_T] = 0$.
}

\lemnn{Strongly orthogonal if and only if $MN$ is U.I.}
\lemnn{For any progressively measurable process such that $\forall~$stopping time $T$, $$\begin{cases}
\mathbb{E}\abs{ M_T } < \infty,\\
\mathbb{E}M_T = 0,
\end{cases}$$
then $M$ is U.I.
}
\defn{stable subspaces of $\mathcal{M}_0^2$}{
    We say $\mathcal{N}_{0} \subseteq \mathcal{M}_{0}^2 $ is a stable subspace if 
    \begin{itemize}
        \item[\textcircled{1}] $N_0$ is closed subspace,
        \item[\textcircled{2}] $\forall~N \in \mathcal{N}_0$, $\forall~$stopping time $T$, we have $N^T \in \mathcal{N}_0$ ($N_t^T = N_{t \wedge T}$). 
    \end{itemize}
}
\ex{}{$c \mathcal{M}_0^2$ is stable.}

\thmnn{
    Let $\mathcal{N}_0$ be a stable subspace of $\mathcal{M}_0^2$. Let $\mathcal{N}_0^\perp$ be the set of $Z \in \mathcal{M}_0^2$ which are weakly orthogonal to every element of $\mathcal{N}_0$, i.e.
    $$
    \mathcal{N}_0^\perp = \{Z \in \mathcal{M}_0^2 \colon \langle Z,N \rangle=0,\,\forall~ N \in \mathcal{N}_0 \}.
    $$
    
    Then $\mathcal{N}_0^\perp$ is a stable subspace of $\mathcal{M}_0^2$, and $\forall~ Z \in \mathcal{N}_0^\perp$, we have $Z,N$ are strongly orthogonal for all $N \in \mathcal{N}_0,$

    Finally, $\forall~ M \in \mathcal{M}_0^2$, $M = N + Z.$
}
\pf{
    $0 = \mathbb{E}(Z_n N) \xlongrightarrow{L^1} \mathbb{E}(ZN).$

    Next, $\forall~ Z \in \mathcal{N}_0^\perp$, then we have $\forall~ N \in \mathcal{N}_{0}$, $\mathbb{E}[Z_\infty N_\infty] = 0$.

    For any stopping time $T$, we have $Z^T \in \mathcal{N}_0^\perp$ because 
    $$
    \begin{aligned}
    \mathbb{E}[Z_\infty^T N_\infty] &= \mathbb{E}[Z_T N_\infty] \\
    &= \mathbb{E}\left[ Z_T \mathbb{E}\left( N_\infty \middle| \mathcal{F}_T \right) \right] &= \mathbb{E}[Z_T N_T]\\
    &= \mathbb{E} \left[ 
        \mathbb{E}\left( Z_\infty \middle| \mathcal{F}_T \right) N_T
     \right]  \\
     &= \mathbb{E}[Z_\infty N_T]\\
     &= \mathbb{E}[Z_\infty N_\infty^T].
    \end{aligned}
    $$
    By $N^T \in  \mathcal{N}_0$, we get $\mathbb{E}[Z_\infty N_\infty ^T] = 0$ by $Z \in \mathcal{N}_0^\perp$. We prove $Z^T \in \mathcal{N}_0^{\perp}\Rightarrow~\mathcal{N}_0^\perp$ is stable.
}

\defnn{
    Define $d \mathcal{M}_0^2 \triangleq (c \mathcal{M}_0^2)^\perp$. $\forall~ M \in \mathcal{M}_0^2$, we write $M = M^c + M^d$ with $M^c \in c \mathcal{M}_0^2$, $M^d \in d \mathcal{M}_0^2$ and $M^c M^d$ is U.I. [by $\mathbb{E}\left[ M_T^c M_T^d \right]=0$.]
}

Define $(\Delta f)(t) = f(t) - f(t-)$.

\thm{Meyer}{
    Let $M \in \mathcal{M}_0^2$. Then $\exists~$unique increasing process $[M]$ null at 0 s.t. 
    \begin{itemize}
        \item[\textcircled{1}] $M^2 - [M]$ is U.I.
        \item[\textcircled{2}] $\Delta[M] = \left( \Delta M  \right)^2.$ 
    \end{itemize}
}
\pf{
    $M_t^2 = (M_t^c + M_t^d)^2 = (M_t^c)^2 + 2M_t^c M_t^d + (M_t^d)^2$.
}

For $M,N \in \mathcal{M}_0^2$, let $[M,N] \equiv \frac{1}{4}\left( [M+N] - [M-N] \right).$

\thm{Kunita \& Watanabe}{
    The process $[M,N]$ is the unique FV. process null at 0 s.t. 
    \begin{itemize}
        \item[\textcircled{1}] $MN - [M,N]$ is U.I.,
        \item[\textcircled{2}] $\Delta[M,N] = \Delta M \cdot \Delta N$. 
    \end{itemize}
}

\section{It\'o Integral}
Let $M \in \mathcal{M}_0^2.$ 

If $H_t = Z \cdot \mathsf{1}_{(S,T]}(t)$ for some $Z \in \mathcal{F}_S$ and $S \leq T$ (stopping times).

Define $\int_{0}^{t} H_s \diff M_s \equiv(H\cdot M)(t) \equiv Z\cdot(M_{T \wedge t} -M_{S \wedge t}).$

We will prove $(H \cdot M) \in \mathcal{M}_0^2.$

\begin{itemize}
    \item[\textcircled{1}] We will prove $\forall~$stopping time $R$ satisfies
    $$
    \begin{cases}
        \mathbb{E} \abs{ (H \cdot M)_{R} } < \infty \\
        \mathbb{E} (H \cdot M)_R = 0.
    \end{cases}
    $$
\end{itemize}
\pf{
    Let $K > 0$ s.t. $\abs{ Z } \leq K < \infty.$ Then $\mathbb{E}\abs{ (H\cdot M)_{R} } \leq K \mathbb{E}\abs{M_{T \wedge R}} + K \mathbb{E}\abs{ M_{S \wedge R} }. (\xredstar{1})$

    By optional stopping 
    $$
    \begin{aligned}
        \mathbb{E}\abs{ M_{T \wedge R} } &= 
        \mathbb{E}\left[ 
            \abs{ \mathbb{E}\left( M_\infty \middle| \mathcal{F}_{T \wedge R} \right) }
         \right]\\
         &\leq \mathbb{E}\left[ 
            \mathbb{E}\left( \abs{ M_\infty } \middle| \mathcal{F}_{T \wedge R} \right)
          \right] = \mathbb{E}\abs{ M_\infty }.
    \end{aligned}
    $$
    So $(\xredstar{1}) \leq 2K \mathbb{E}\abs{ M_\infty } < \infty.$

    To prove $\mathbb{E}(H\cdot M)_{R} = 0 \Leftrightarrow \mathbb{E}\left[ 
        Z(M_{T \wedge R} - M_{S \wedge R})
     \right] = 0. (\xredstar{2})$

    We may replace $Z$ by $\mathsf{1}_A$ for $A \in \mathcal{F}_S.$ Then 
    $$  
    \xredstar{2} \Leftrightarrow \mathbb{E}\left[ \mathsf{1}_A(M_{T \wedge R} - M_{S \wedge R}) \right] = 0.
    $$

    Define $$S_A \equiv \begin{cases}
        S, &\text{on }A,\\
        \infty,&\text{on }A^C.
    \end{cases}$$Then 
    $$
    \mathbb{E}\left[ \mathsf{1}_A(M_{T \wedge R} - M_{S \wedge R}) \right]\Leftrightarrow \mathbb{E}\left[ M_{T_A \wedge R} - M_{S_A \wedge R} \right] = 0.
    $$
}

\lemnn{
    If $Z \in b \mathcal{F}_S$ and $M$ is U.I. martingale. Then $H\cdot M$ is U.I. martingale.
}

Next, consider $H_t = \sum_{i=1}^n Z_i \mathsf{1}_{(S_i, T_i]} (t)$.

$(H\cdot M)_{t} = \sum_{i=1}^{n} Z_i (M_{T_i \wedge t} - M_{S_i \wedge t})$.

\cor{
    If $Z_i \in b \mathcal{F}_{S_i}$ and $M$ is U.I. martingale, then $(H\cdot M)_{t}$ is U.I. martingale.
}

\defn{previsible process}{
    Previsible $\sigma-$algebra $\mathsf{P}$ on $(0,\infty) \times \Omega$ is defined to be the smallest $\sigma$-algebra s.t. every adapted $L$-process is $\mathsf{P}$-measurable.

    $L$-process: $(X_t)_{t > 0}$ s.t. $X_t = X_{t-}$ \& $X_{t+}$ exists. 
    
    We say $(X_t)_{t > 0}$ is previsible if it is $\mathsf{P}$-measurable.
}
\lemnn{
    Suppose $S$ and $T$ are stopping times with $S \leq T \leq \infty$. Let $Z \in b \mathcal{F}_{S}$ and $H = Z \cdot\mathsf{1}_{(S,T]}$. Then $H$ is previsible.
}
\pf{
    $\forall~ t > 0$ and $B$ Borel set, $\{H_t \in B\} \in \mathcal{F}_t$. We may assume $0 \not\in B.$
    
    Then $\{H_t \in B\}= \{Z \in B\} \cap \{S < t \leq T\} = \{Z \in B\} \cap \{S < t\} \cap \{t \leq T\}$.

    Note that
    $$\{S < t\} = \bigcup_{n=1}^\infty \{S \leq t - \frac{1}{n}\}$$.

    Hence 
    $$
    \{H_t \in B\} = \bigcup_{n=1}^\infty\left( \{Z \in B\} \cap \{S \leq t - \frac{1}{n}\}  \right) \cap \{T \geq t\} \subseteq \mathcal{F}_{t - \frac{1}{n}} \subseteq \mathcal{F}_t.
    $$
}

\defnn{
    $\displaystyle b \varepsilon = \left\{\sum_{i=1}^n Z_i \mathsf{1}_{(S_i,T_i]} \colon n \geq 1, Z_i \in b \mathcal{F}_{S_i}, S_1 \leq T_1 \leq S_2 \leq \cdots.\right\}$
}

\noindent\textbf{claim:} $\sigma(b \varepsilon) = \mathsf{P}.$

\pf{
    ``$\subseteq$'' done.

    ``$\supseteq$'' For any bounded left-continuous process,
    $$
    H_t = \lim_{k \uparrow \infty} \lim_{n \uparrow \infty} \sum_{i=2}^{n_k} H_{\frac{i-1}{n}} \mathsf{1}_{\left(\frac{i-1}{n},\frac{i}{n}\right]}(t).
    $$

    Since $H$ is adapted, we get $H_{\frac{i-1}{n}} \in b\mathcal{F}_{\frac{i-1}{n}}$.

    Hence
    $$
    \sum_{i=2}^{n_k} H_{\frac{i-1}{n}} \mathsf{1}_{\left( \frac{i-1}{n}, \frac{i}{n} \right]}(t) \in b \varepsilon.
    $$  

    $\implies H_t \in \sigma(b \varepsilon).$
}

\lemnn{
    Let $H \in b \varepsilon$, $M \in \mathcal{M}_{0}^2$. Then $H \cdot M \in \mathcal{M}_0^2$, and 
    $$
        \mathbb{E}(H \cdot M)_\infty^2 = \sum_{i=1}^{n}\mathbb{E}\left[ Z_{i-1}^2(M_{T_i} - M_{T_{i-1}})^2 \right]
    $$
    if $H = \sum_{i=1}^n Z_{i-1} \mathsf{1}_{(T_{i-1},T_i]}$.
}

\pf{
    $(H \cdot M)_t = \equiv \sum_{i=1}^n Z_{i-1}\left( M_{T_i \wedge t} - M_{T_{i-1} \wedge t} \right)$. We have $(H \cdot M)$ is U.I. martingale.

    Then $(H\cdot M)_{t} \to (H\cdot M)_{\infty}$ a.s. and in $L^1$, where $(H \cdot M)_{\infty} = \sum_{i=1}^n Z_{i-1}\Delta i$, $\Delta_i = M_{T_i} - M_{T_{i-1}}.$

    Notice that 
    $$
    \mathbb{E}\left[ (H \cdot M)_{\infty}^2 \right] = \mathbb{E} \sum_{i=1}^n Z_{i-1}^2 \Delta_i^2 + 2 \mathbb{E}\left( \sum_{i < j} Z_{i-1}\Delta_i Z_{j-1}\Delta_j \right).
    $$

    For $i \leq j-1 < j$, we get
    $$
    \mathbb{E}\left[ Z_{i-1} \Delta_i Z_{j-1}\Delta_j\right] = \mathbb{E}\left[ Z_{i-1}\Delta_i \cdot Z_{j-1} \mathbb{E}\left[ \Delta_j \middle| \mathcal{F}_{T_{j-1}} \right] \right] =0.
    $$

    By Doob's $L^2$ ineq.

    $$
    \mathbb{E}\left[ 
        \sup_{t \geq 0}\left( H \cdot M \right)_{t}^2
     \right] \leq 4 \mathbb{E}\left[ (H \cdot M)_{\infty}^2  \right] < \infty.
    $$
    $\implies H \cdot M \in \mathcal{M}_0^2.$
}

Next, Recall that 
$M \in \mathcal{M}_0^2 \implies \exists~$quadratic variation $[M]$ s.t. $M^2 - [M]$ is U.I..
$$
    \mathbb{E}\left[ (H\cdot M)_{\infty}^2 \right] = \sum_{i=1}^n \mathbb{E} \left[ 
        Z_{i-1}^2 \left( M_{T_i} - M_{T_{i-1}} \right)^2
     \right]. \xredstar{1}
$$

Note
$$
    \begin{aligned}
    \mathbb{E}\left[ 
        \left( M_{T_i} - M_{T_{i-1}} \right)^2 \middle| \mathcal{F}_{T_{i-1}}\right] &= \mathbb{E}\left[ 
            M_{T_{i}}^2 - M_{T_{i-1}}^2 \middle| \mathcal{F}_{T_{i-1}}
         \right]\\
         &= \mathbb{E}\left[ 
            [M]_{T_i} - [M]_{T_{i-1}} \middle| \mathcal{F}_{T_{i-1}}
          \right].
    \end{aligned}
$$
So
$$
    \xredstar{1} \implies \sum_{i=1}^{n} \mathbb{E}\left[ 
        Z_{i-1}^2 \left( [M]_{T_i}-[M]_{T_{i-1}} \right)
     \right] = \mathbb{E}\left[ 
        \int_{0}^{\infty}H_s^2 \diff [M]_s 
      \right].
$$
$$
    \int_{0}^\infty \left( \sum_{i=1}^\infty Z_{i-1} \mathsf{1}_{(T_{i-1} - T_i]} \right)^2 \diff [M]_s = \int_{0}^\infty \sum_{i=1}^n Z_{i-1}^2 \mathsf{1}_{(T_{i-1},T_i]} \diff [M]_s.
$$

$\mathbb{E}(H \cdot M)_{\infty}^2 = \mathbb{E} \left[ \int_{0}^\infty  H_s ^2 \diff  [M]_s\right] < \infty.$

For any fixed $M \in \mathcal{M}_0^2$, define 
$$
    \norm{ H }_{M} = \left( \mathbb{E}\int_{0}^{\infty} H_s ^2 \diff [M]_s  \right)^{\frac{1}{2}}.
$$

Then $L^2(M) \equiv \left\{ \text{previsible }H \text{ with } \norm{ H }_{M} < \infty \right\}$.

We have
$$
\begin{aligned}
    I: L^2(M) \cap b \varepsilon &\to L^2(\mathcal{F}_{\infty})\\
    H & \mapsto (H\cdot M)_{\infty}.
\end{aligned}
$$

$\norm{ H }_{M} = \left( \mathbb{E}\left[ \int_{0}^\infty H_s ^2 \diff [M]_s \right] \right)^{\frac{1}{2}}$.

$\norm{ I(H) } = \left( \mathbb{E}(H \cdot M)_{\infty}^2 \right)^{\frac{1}{2}}.$

And $\norm{ H }_{M} = \norm{ I(H) }$, we get isometry:
$$
\mathbb{E}\left[ \int_{0}^\infty H_s ^2 \diff [M]_s \right]=\mathbb{E}(H \cdot M)_{\infty}^2.
$$
$\implies$exstends uniquely to $L^2(M) \cap b \varepsilon = L^2(M)$.

\defn{It\'o Integral}{
    Let $M \in \mathcal{M}_{0}^2$ and $H \in L^2(M)$. Then It\'o Integral $(H\cdot M)_{t}$ is the image of $H$ under extension of $I : L^2(M)\to L^2(\mathcal{F}_{\infty})$.
}

\noindent\textbf{Localization}
\defnn{
    Let $T$ be a stopping time and $H$ is any process. Define 
    $$
        H(0,T](t) = \begin{cases}
            H_t, & 0 <t \leq T ,\\
            0, & t >T.
        \end{cases}
    $$
}

\rmkb{
    If $H$ is previsible, so is $H(0,T].$
}

\defnn{
    Let $T$ be a stopping time and $X$ is any process. Define
    $$
    X^T(t) = \begin{cases}
        X(t),& 0 \leq t \leq T,\\
        X(T), &t > T.
    \end{cases}
    $$
}

Then $(H \cdot M)^T = (H(0,T]\cdot M^T)_t,$ and 
$$
    \left[ Z_i \left( M_{T_i \wedge t} - M_{T_{i-1} \wedge t} \right) \right]^T = \left( Z_i \left( T_{i-1}\wedge T , T_i \wedge T \right]\cdot M^T \right)_t = Z_i\left( M_{T_i \wedge T \wedge t} - M_{T_{i-1}\wedge T \wedge t} \right).
$$

\thmnn{
    Fix $M \in \mathcal{M}_0^2$. For any $H \in L^2(M)$ and any stopping time $T$.
    \begin{itemize}
        \item[(i)] $\displaystyle (H \cdot M)^T = H(0,T] \cdot M = H \cdot M^T.$
        \item[(ii)] $(H\cdot M)_t ^2 - \int_{0}^\infty H_s^2 \diff [M]_s$ is U.I. martingale. $\implies [H\cdot M]_t = \int_{0}^t H_s^2 \diff [M]_s$.
        \item[(iii)] If $H, K \in b \mathsf{P}$, then $(HK)\cdot M = H\cdot (K\cdot M)=K \cdot (H \cdot M).$ 
        \item[(iv)] $\Delta(H \cdot M) = H \cdot \Delta M$.
        \item[(v)] If $M$ is cts, then $H \cdot M$ is cts.  
    \end{itemize}
    
}
\pf{
    (i) $(H \cdot M)_t^T = (H \cdot M)_{t \wedge T}.$
}

\ex{}{
    $H = Z \mathsf{1}_{(U,V]}$.

    $(H\cdot M)_t = Z (M_{V \wedge t} - M_{U \wedge t}) \implies (H \cdot M)_{t \wedge T} = Z\left( M_{V \wedge T \wedge t} - M_{U \wedge T \wedge t} \right).$

    $H(0,T](t) = Z \mathsf{1}_{(U \wedge T , V \wedge T]}(t).$

    So $(H(0,T] \cdot M)_{t} = Z(M_{V \wedge T \wedge t} - M _{U \wedge T \wedge t}).$

    $(H \cdot M^T)_t = Z (M_{V \wedge t}^T - M_{U \wedge t}^T) = Z (M_{T \wedge V \wedge T} - M_{T \wedge U \wedge t}).$
}

Hence it holds for $H \in b \varepsilon. \implies$ holds for all $H \in L^2(M).$

$(H \cdot M)_{\infty}^T = Z(M_V - M_U).$

\pf{
    (ii) $N_t = (H\cdot M)_{t}^2 - \int_{0}^t H_s ^2 \diff [M]_s.$
    $$
    \mathbb{E}\left[ \sup_{t \geq 0} (H \cdot M)_t ^2 \right] \leq 4 \mathbb{E}\left[ 
        \int_{0}^{\infty}H_s^2 \diff [M]_s
     \right]< \infty.
    $$
    Hence $\mathbb{E}\left[ \sup_{t \geq 0} \abs{N_t} \right] < \infty \implies \{N_t, t \geq 0\}$ is U.I..

    It suffices to prove $\forall~$stopping time $T$, 
    $$
    \begin{cases}
        \mathbb{E}[N_T] = 0 \\
        \mathbb{E}\abs{N_T} < \infty
    \end{cases} \implies \text{ U.I. martingale.}
    $$
    The second condition is proved, Now we need to check the first condition.
    $$
    \mathbb{E}[N_T] = \mathbb{E}\left[ (H \cdot M)_{T}^2 \right] - \mathbb{E} \int_0^T H_s^2 \diff [M]_s.
    $$

    Notice that $(H \cdot M)_{T} =Z(M_{V \wedge T} - Z_{U \wedge T})= (H \cdot M)_\infty^T$.

    Then 
    $$
    (H \cdot M)_{T} = (H \cdot M)_{\infty}^T = \left( H(0,T] \cdot M \right)_{\infty}
    $$  
    $$
    \implies \mathbb{E}\left[ (H\cdot M)_{T}^2 \right] = \mathbb{E} \left[ (H(0,T] \cdot M)_{\infty}^2 \right] = \mathbb{E}\left[ 
        \int_{0}^\infty \left( H(0,T]_{s} \right)^2 \diff [M]_s
     \right] = \mathbb{E}\left[ 
        \int_0^T H_s^2 \diff [M]_s.
      \right]
    $$
}

\pf{
    (iii) $H = Z_1 (S_1,T_1]$, $K = Z_2 (S_2,T_2].$

    $HK = Z_1 Z_2 (S_1 \vee S_2,(T_1 \wedge T_2) \vee (S_1 \vee S_2)].$
    $$
    \left( \left( HK \right)\cdot M \right)_{\infty} = Z_1 Z_2 \left( M_T - M_S \right).
    $$
    $$
    \begin{aligned}
        \left( H \cdot \left( K \cdot M \right) \right)_{\infty} &= Z_1 \left( \left( K \cdot M \right)_{T_1} - \left( K \cdot M \right)_{S_1} \right) \\
        &= Z_1 \left[ 
        (K\cdot M)_{\infty}^{T_1} - \left( K \cdot M \right)_{\infty}^{S_1}
     \right] \\
     &= Z_1 \left[ (K \cdot M^{T_1})_{\infty} - (K \cdot M^{S_1})_{\infty} \right]\\
     &=Z_1\left[ 
        Z_2 \left( M_{T_2}^{T_1} - M_{S_2}^{T_1} \right) - Z_2 \left(M_{T_2}^{S_1} - M_{S_2}^{S_1}\right)
      \right]\\
      &= Z_1 Z_2 \left[ M_{T_2 \wedge T_1} -M_{S_2 \wedge T_1} - \left( M_{T_2 \wedge S_1} - M_{S_2 \wedge S_1} \right) \right]\\
      &= Z_1 Z_2 \left( M_T -M_S \right).
    \end{aligned}
    $$
    So $H\cdot(K\cdot M) = (H\cdot K)\cdot M$, $\forall~ H,K \in b \varepsilon.$

    Next, for any $k \in b \mathsf{P}$ s.t. $\exists~ k^n \in b \varepsilon,$ $k^n \xlongrightarrow{L^2(M)}k$.

    Then $\forall~ H \in b \varepsilon$,
    $$
    \mathbb{E}\left( 
        \left( H \cdot(K \cdot M) - H\cdot (k^n \cdot M) \right)_{\infty}^2
     \right) = \mathbb{E}\left( 
        \left( H \cdot (k-k^n)\cdot M \right)_{\infty}^2
      \right)=\mathbb{E}\left[ 
        \int_{0}^\infty H_s^2 \diff [(k-k^n)\cdot M]_s
       \right].
    $$

    \textbf{Recall:} $$[H \cdot M]_t = \int_{0}^t H_s^2 \diff [M]_s = \mathbb{E}\left[ \int_{0}^\infty H_s^2 (k_s - k_s^n)^2 \diff [M]_s \right] \to 0,$$
    by $\abs{ H_s } \leq C$ and $k^n \xlongrightarrow{L^2(M)} k$.
    $$
    \norm{ k^n - k }_{M}^2 \triangleq \mathbb{E}\left[ \int_{0}^\infty(k_s - k_s^n)^2 \diff [M]_s \right] \to 0.
    $$

    Hence $\mathbb{E}\left[ \left( (H \cdot (k^n \cdot M)) - H \cdot(K \cdot M) \right)_{\infty}^2 \right] \to 0.$

    Next 
    $$
        \mathbb{E}\left[ H\cdot (k^n \cdot M)_{\infty}^2 \right] = \mathbb{E}\left[ 
            \left( \left( H\cdot k^n \right)\cdot M \right)_\infty^2
         \right] \to \mathbb{E}\left[ 
            \left( \left( HK \right)\cdot M \right)_{\infty}^2
          \right].
    $$

    We conclude 
    $$
    \mathbb{E} \left[ 
        (H \cdot (k \cdot M))_{\infty}^2
     \right] = \mathbb{E}
     \left[ 
        ((Hk) \cdot M)_{\infty}^2
      \right].
    $$
}

\pf{
    (iv) If $H = Z(S,T]$, then $(H \cdot M)_{t} = Z\left( M_{t \wedge T} - M_{t \wedge S} \right)$.
    $$
    \Delta (H \cdot M)_t = \Delta(Z (M_t - M_S)) = Z \times \Delta M_t = H \times \Delta M_t.
    $$

    $\implies$holds for all $H \in b \varepsilon.$

    Next, $\forall~ H \in L^2(M)$, we let $H^n \in b \varepsilon$ and $H^n \xlongrightarrow{L^2(M)} H.$ Then 
    $$
        H^n \cdot M \xlongrightarrow{L^2(\mathcal{F}_{\infty})} H \cdot M \Longleftrightarrow H^n \cdot M \xlongrightarrow{\mathcal{M}_0^2} H \cdot M.
    $$
    
    $\mathbb{E}\left[ \int_{0}^{\infty}(H_S^n - H_S)^2 \diff  [M]_s \right] \to 0 \implies \exists~$subseq $n_k \to \infty$ s.t. W.P.1.
    $$
    \int_0^\infty(H_S^n - H_S)^2 \diff [M]_s \to 0.
    $$
    $$
    \sup_{t \geq 0}\abs{ (H^n \cdot M - H \cdot M)_t }\xlongrightarrow{L^2} 0 \implies \sup_{t \geq 0} \abs{ (H^{n_k} \cdot M)_t - (H\cdot M)_t }\xlongrightarrow{\text{a.s.}}0.
    $$

    By $\Delta(H^{n_k} \cdot M)_t = H^{n_k} \cdot \Delta M_t$.

    \textbf{case 1.} If $\Delta M_S = 0$, then $\Delta(H^{n_k} \cdot M)_S = 0.$

    By uniform convergence $\Delta (H \cdot M)_S =0.$

    \textbf{case 2.} If $\Delta M_S \neq 0$, then $\Delta [M]_S = (\Delta M_S)^2 > 0$.

    Then $H_S^{n_k} - H_s \to 0$ by 
    $$
    0\leftarrow\int_{0}^{\infty}\left( H_t^{n_k} - H_t \right)^2 \diff  [M]_t \geq \left( H_S^{n_k} - H_S \right)^2 \cdot \Delta[M]_S.
    $$

    $\Delta(H^{n_k} \cdot M)_S = H_S^{n_k} \cdot \Delta M_S$, let $k \to \infty $ we have
    $$
    \Delta(H \cdot M)_S = H_S \cdot \Delta M_S.
    $$
}